# CUDA 算子调优系列 · 路线图（活文档）

> 这是「CUDA 算子调优」系列持续自驱工作的**控制面板**。AI agent 每次接管时：
> 先读本文 → 读 `agent_guide.md` → 读 [agent skill](../../agent_skills/kernel-opt.md) → 从「下一步」做起。
> **配套台账：[TECHNIQUES.md](TECHNIQUES.md)**（技巧记录 + 性能榜，每篇必须往里加条目）。

## 目标

一条**由浅入深、每篇都能自己跑出来**的算子调优学习线，最终覆盖两件事：

1. **通用 CUDA 优化**（01–14 已完成）：访存、共享内存、归约、GEMM、Tensor Core、异步流水线、融合。
2. **模型场景算子**（15 起，重点）：把 DeepSeek-V4.x / Kimi-K2.6 / Qwen3 / GLM 等真实模型里的
   MLA、DSA 稀疏注意力、MoE、FP8 GEMM、MuonClip 等算子**自己写出来并推到极致**；
   建立**技巧台账 + 性能榜**，把每一次优化的手段、原理、实测收益记录下来。

**北极星指标（持续逼近）**：

- 内存受限算子：≥ 90~95% HBM 带宽；
- GEMM（bf16 TC）：≥ 60% 峰值 / ≥ 80% 同口径 cuBLAS；
- attention / MLA / DSA：对标 FlashMLA / 随模型规模可用的 SOTA 实现，给出差距百分比；
- 每个算子都要有「优化前 → 优化后」的真实数字，并进入 `TECHNIQUES.md` 性能榜。

原则：

1. **代码必须能跑**：每个实验都有 `__main__` 式自测 / 参考实现对拍，实测数据写进文章与台账。
2. **数据必须真实**：数字来自本机 H100 实测（记录环境、命令、原始输出）；不允许编造。
3. **先说人话**：讲清楚「为什么慢 → 怎么想到的 → 改了什么 → 快了多少」。
4. **对标 SOTA**：能量化就量化（cuBLAS / CUTLASS / FlashMLA / DeepGEMM / flashinfer）。
5. **持续演进**：路线图是活文档，跑出新发现就调整；**未完成项用完后自行补充新任务**。

## 硬件 / 环境 / 本地资料

| 项 | 值 |
|---|---|
| GPU | 8 × NVIDIA H100 80GB HBM3 (SXM)，CC 9.0，132 SM，HBM ~3.35 TB/s，BF16 TC dense ~989 TFLOPS，FP8 ~1978 TFLOPS |
| 容器 | `kernel_lab`（镜像 `dsv4-inf:latest`，CUDA 13.2，含 nvcc / ncu / nsys / PyTorch） |
| 关键点 | 宿主 `RmProfilingAdminOnly=1`，普通容器跑 ncu 报 ERR_NVGPUCTRPERM；`kernel_lab` 加了 `SYS_ADMIN`/`SYS_PTRACE` |

```bash
cd ~/proj/tech_record/code/kernel-opt       # /home/xieminglin 是指向 /ssd/home/xieminglin 的软链
scripts/lab.sh up                           # 确保容器在跑（幂等）
scripts/run.sh 15-mla-attn/mla_attn.cu      # 编译 + 运行
scripts/ncu.sh 15-mla-attn/mla_attn.cu --set full --kernel-name regex:mla
```

**本地参考仓库**（`~/github/`）：`FlashMLA`、`DeepGEMM`、`DeepEP`、`cutlass`、`flashinfer`、
`flash-attention`、`tilelang`、`triton`、`Liger-Kernel`、`muonclip`、`TransformerEngine`、`vllm`、
`How_to_optimize_in_GPU`、`CUDA_C_Programming_Guide.pdf`。

**本地模型**（`/ssd/models/`，可读 `config.json` 拿真实算子形状）：

| 模型 | 关键参数（用于构造 shape） |
|---|---|
| DeepSeek-V4-Pro | MLA: heads=128, kv_head=1, head_dim=512(qk_rope=64), q_lora=1536；DSA: index_n_heads=64, index_head_dim=128, index_topk=1024, compress_ratios∈{128,4,0}；MoE: 384 routed + 1 shared, top-6, moe_inter=3072；FP8 e4m3 + ue8m0, weight_block 128×128 |
| DeepSeek-V4.1-Flash | model_type=deepseek_v41；FP8 dynamic, ue8m0, weight_block 32×32, expert_dtype fp4 |
| Kimi-K2.6 | MLA（te-perf 已测 shape `(1,4096,64,192,128)`，qk=192, v=128）；kimi_k25 配置 |
| Qwen3 系列 | GQA：q=40/kv=8、32/4、64/4，head_dim=128（te-perf 已列） |

---

## 系列大纲

状态：`[ ]` 未开始 · `[~]` 进行中 · `[x]` 已完成并发布 · `[-]` 暂缓。

### 第一部分~第四部分（通用优化，01–14 已完成）

- [x] **01** 开篇：执行模型 / roofline / 工具链
- [x] **02** 第一个 kernel：线程层次 / vector add 三写法
- [x] **03** 正确测量：计时陷阱 / 有效带宽 / ncu 入门
- [x] **04** 访存合并与向量化：copy 行/列 7.4×
- [x] **05** 共享内存与 bank conflict：矩阵转置 466→1651 GB/s
- [x] **06** 归约与 warp shuffle：0.1%→91%
- [x] **07** Softmax 优化：多趟→融合→smem 85.3%
- [x] **08** GEMM 入门：naive→smem tiled
- [x] **09** GEMM 进阶：寄存器分块 + cp.async 双缓冲 51.5%
- [x] **10** ncu 深潜：occupancy / stall / roofline / SASS
- [x] **11** launch 配置与 occupancy：ILP × occupancy / `__launch_bounds__`
- [x] **12** 异步拷贝与流水线：cp.async N 级软流水 53.6%
- [x] **13** Tensor Core 入门：WMMA / `mma.m16n8k16` / ldmatrix 221 TFLOPS
- [x] **14** 融合与 epilogue：GEMM+bias+GELU 仅慢 1% / split-K

### 第五部分：模型场景算子（15 起，重点）

- [x] **15 MLA 注意力（一）**：DeepSeek/Kimi MLA 数学（q_lora/kv_lora、decoupled RoPE、absorb）· 三个标量实现 + roofline（证明 prefill 算力受限）· 真实 shape 台账 · TC 三 kernel 115.6 TFLOPS
- [x] **16 MLA 注意力（二）**：FlashMLA 式单 kernel 融合。online softmax 融进 QK epilogue（S 不落盘）· 累加器 C→PV A 片段零 shuffle · KV 常驻 smem · `f4`(dup2) 146.5 → **`f4s` 共享 P 消重复 170.1 TFLOPS（S=1024）/ 184.7（Sk=4096）** · wgmma(INTERLEAVE) 仅 84.6（暴露 swizzle 才是胜负手）· 距 FlashMLA 640 从 5.7× 收到 3.5×
- [x] **17 DSA 稀疏注意力（DeepSeek-V4）**：**（一）= 文章 18**：lightning indexer（64×128）打分（287–311 TFLOPS）+ exact top-k radix-select（6.75ms @32k）；**（二）= 文章 19**：稀疏 MLA 消费端（gather top-k + 尾部掩码 + 共享 P + `cp.async` 双缓冲），132–137 TFLOPS，sparse attention 相对稠密 4k 3.0× → 64k 48.6×，DSA 端到端 4k 2.5× → 64k **17.3×**。**（三）= 文章 35**：KV compressor（门控池化，ratio 128/4），合并投影 650–655 TFLOPS + 融合池化 83–84% HBM，端到端相对 eager 2.0–2.4×；投影被 L2 卡住（见「下一步」）。**剩余**：indexer 的 compressor（ratio=4/head_dim=128/rotate+FP4）。
- [x] **18 RoPE 融合算子**：yarn scaling（beta_fast/slow, factor 16）+ fused rotary，对照非融合 —— **已作为文章 53 发布**（见「当前进度」）：纯访存，预表后交错配对 `uint4` 流 **78.6%**（模式 roof 99.5%）/ 对半配对 **82.1%**（98.0%）；上限是访问模式（rope 64/512 的 1 KB 步长），打包连续 86.4%；torch 22.4×/6.4×
- [x] **19 RMSNorm / QK-Norm 融合**：DeepSeek/Kimi RMSNorm、QK-norm、fused residual+norm，冲 HBM 峰值 —— **已作为文章 32 发布**（见「当前进度」）：寄存器缓存整行消 smem 往返，`v2r` **87.2% HBM**；再叠 FP8 per-128 量化掉到 65.8%（amax atomic 墙）
- [x] **20 MoE（一）：router + top-k + permutation**：384 experts top-6，token permute/unpermute，测路由与搬运开销 —— **已作为文章 27 发布**（见「当前进度」）
- [x] **20b gate GEMM 极限冲刺：wgmma+TMA+WS**：把 27 的 gate 从 cuBLAS 的 28% 做到 77–80%（文章 28）；剩 A 重读的 cluster multicast，见「下一步」
- [x] **21 MoE（二）：grouped GEMM**：变长 group 的 grouped GEMM，对照 `~/github/DeepGEMM` 的 contiguous/masked 分组 —— **已作为文章 25 发布**（见「当前进度」）
- [x] **21c MoE（二·续）：grouped GEMM + per-block FP8 缩放**：把 DeepSeek 真实的 `e4m3 + 128×128 weight_block` 叠进 grouped 骨架 —— **已作为文章 31 发布**（见「当前进度」）：per-block 806.4（32k）= 同二进制 per-tensor 的 82.2%，损失小于稠密单 GEMM 的 23%；decode 上免费（85.4% HBM）
- [x] **22 FP8 GEMM（一）：per-tensor / per-block scaling**：e4m3 + ue8m0 缩放，weight_block 128×128 —— **已作为文章 22 发布**（见「当前进度」）
- [x] **23 FP8 GEMM（二）：TMA + mbarrier + warp specialization**：已完成并发布（见「当前进度」）。e4m3 per-tensor 768→**1217 TFLOPS（cuBLAS 的 88.1%）**；per-block 519→906。
- [x] **23b FP8 GEMM（三）：per-block 缩放的寄存器墙 + ptxas 序列化**（= 文章 24）：已完成并发布。同二进制 per-tensor 对照 128×128 s3 **1090.8** / 256×128 s4 **1209.6**；per-block 最佳 **936.5（128×128 s4，47.3%）**。根因：`fin` 多 64 regs（90→154）→ occupancy **2→1 CTA/SM**（Compute 67.7%→49.7%）；BM=256 的 per-block 累加器 `512×128=65536` 恰为整个 regfile（96 regs + 608B spill → 224）；ping-pong/pair-drain 触发 ptxas `C7514/C7511` 串行化（358/397）；scale 预取与强制 2 CTA 均负结果。**剩余**：1-warpgroup/248-reg 布局、cluster multicast、expert fp4，见 backlog。**已由文章 37 换路解决**：`ue8m0` 是 2 的幂 → 把 scale 折进操作数、切 per-tensor 路径，实测 940.6→1207.9 TFLOPS（1.28×）。
- [x] **24 Muon / MuonClip 优化器算子**：Newton–Schulz 迭代做正交化（zeropower）+ clip 融合；参考 `~/github/muonclip`；N=4096 级矩阵，冲 TFLOPS → **已作为系列第 17 篇发布**（见「当前进度」）
- [x] **25 Paged KV-cache / flash-decoding 推理注意力**：GQA/MQA、block table、变长 seqlen；参考 `flashinfer`/`vllm` —— **文章 38（GQA decode，92.8% HBM）** 已完成；**MLA/MQA decode 见 25b（文章 39）**。
- [x] **25c MLA decode 的 wgmma 改造**：纯 wgmma 撞上 V 转置税（181<190）→ 混合（wgmma QK + mma/`ldmatrix.x4.trans` PV 直读 SW128 K tile）+ K 双缓冲 → **290.7 TFLOPS（39 的 1.53×）**。**文章 42**。剩余：producer/consumer WS 消 `wait`、MN-major 描述符让 PV 回到 wgmma（文章 44）。
- [x] **26 量化推理算子：W4A16 dequant-GEMM（GPTQ/AWQ）**：ERNIE/GLM/Kimi 量化部署常用；dequant 融合进 GEMM —— **已作为文章 40 发布**（见「当前进度」）：int4 权重 + bf16 激活，融合版在 `M=64` decode **1.38×**（0.1114 vs 0.1536ms），但反量化税 46%、`Work_dequant ∝ (M/BM)NK` 导致 M≥128 反转；有效权重带宽 400 GB/s vs Marlin 类 SOTA 差 ~6.7×
- [x] **26c W4A16 的跨 item 持久化流水**：展平 `(item,kb)` 一维 stage 流让 producer/consumer 跨 item 不停机；`M=1` wave 2.58→1、565.5 GB/s（41 篇的 1.04×）—— **已作为文章 43 发布**（见「当前进度」）
- [x] **26d W4A16 的 M=1 GEMV（形状随 work point）**：算清 `AI≈3.76M`、张量核要到 `M≈78` 才划算；M=1 改「每 warp 一行 + warp 内沿 K 并行 + `shfl` 归约 + x/scale 常驻 smem + `__ldcs`」，1566.6 GB/s / 49.7% HBM，相对 43 篇 tensor-core **2.81×** —— **已作为文章 44 发布**（见「当前进度」）
- [x] **26e W4A8 + `dp4a`（M=1 GEMV）** —— **已作为文章 45 发布**（见「当前进度」）
- [x] **26f 小 M（M∈[1,64]）的 GEMV ↔ 张量核分派** —— **已作为文章 46 发布**（见「当前进度」）
- [x] **26g W4A16 小 M 的 warp specialization（负结果）+ KSPLIT 并行度**：WS 不能减少那 78% 的反量化 ALU/FMA 指令，只让 `long_scoreboard` 恶化（1.77→10.19）；真正的杠杆是 `KSPLIT=8→16`（waves 2.06→4.12）在 `M∈[3,16]` +5~11%。新自适应 1.23× always-wgmma —— **已作为文章 47 发布**（见「当前进度」）
- [x] **26h W4A8 的整数张量核（IMMA）小 M GEMM**：换数制——`mma.sync.m16n8k32.s32.s8.u8.s32` + 激活 int8 per-128 + 权重 nibble 铺 int8，片段每 lane 一个 word 免 `ldmatrix`；uint16 直读 + `PRMT` + `u8.s8` 免 `-8`；去掉 smem scale 表。M=16 权重带宽 650→**1290 GB/s（19.4%→38.5% HBM）**，相对 bf16 **1.92×**、全 M 段 **1.80×**，指令 −59%、数值精确 1e-7 —— **已作为文章 48 发布**（见「当前进度」）
- [ ] **27 Attention 反向（FA bwd）**：自己推 dQ/dK/dV 并写 kernel，对拍 autograd（呼应 flash-attention 系列）
- [x] **28 融合 cross-entropy / logits**（vocab=129280）：分块在线 logsumexp，省一次全量读写 —— **已作为文章 54 发布**（见「当前进度」）：split-N TMA+wgmma GEMM + tile 级 online logsumexp + superblock L2 swizzle，`sw 4×8` **25.07 ms / 605.7 TFLOPS（61.2% 峰值）**，logits 显存 2.12/4.24 GB → **66 MB**；vs cuBLAS+CE 0.98×、eager 0.92×、fp32-logits 1.16×。判决：大词表 compute-bound LM head 上融合 CE 是**显存优化不是吞吐优化**（logits 往返仅占 GEMM 流量 2.4%）。**剩余**：把 V4 的 FP8 LM head 用 FP8 wgmma 做（权重字节减半 + L2 流量 −17%）——**已作为文章 55 完成**：fp8 融合 CE 12.82 ms / 1184.6 TFLOPS（59.9% FP8 峰值），比 bf16 融合 1.95×；**剩余**：真正的 per-128×128 block 量化器 + 真实 outlier 权重上的精度，见「下一步」。
- [x] **29 MoE（三）：fused MoE（grouped GEMM + 激活 + unpermute）** 端到端对照 —— **已作为文章 30 发布**（见「当前进度」）：3 kernel、1.02–1.08×、activation −81%；教训=prefill FFN 权重带宽受限。遗留：per-block FP8 叠加（下一步 31）
- [ ] **30 选择性扫描 / SSM 算子**（Hy3 / Mamba 混合模型）：并行 scan kernel

### 第六部分：极致性能（把上面的算子推到极致）

- [~] **31 wgmma（Hopper warpgroup MMA）**：用 `wgmma.mma_async` 替换 `mma.sync.m16n8k16`，目标 GEMM ≥ 400 TFLOPS。**已做（文章 20）**：SW128 swizzle 冒烟 GEMM 132、MLA 105.3（+24.5% vs INTERLEAVE）；剩 TMA/多级流水/warp specialization 才能冲高，见「下一步」。
- [x] **32 TMA + mbarrier 多级流水**：`cp.async.bulk.tensor.2d` + tensormap，替换 cp.async —— **GEMM 上已完成（文章 23）**；attention/MLA 上仍待做（V 需转置，见 backlog）
- [x] **33 Warp specialization（生产者/消费者）+ ping-pong 调度**：**FA8 GEMM 上已完成（文章 23）**，1 producer warp + N consumer warpgroup，mbarrier full/empty 握手，张量管线活跃度 72%
- [ ] **34 Persistent kernel + Stream-K**：为 tall-skinny / 不规则 M×N 做工作分解
- [ ] **35 Split-K / parallel-K**：小 M/N、大 K 的 GEMM
- [ ] **36 Autotuning 台 + 性能回归看板**：系统扫 launch 配置/流水级数，最佳结果写入 TECHNIQUES.md
- [ ] **37 CUTLASS / CuTe 对照**：复现一个 CUTLASS 例程并在同 shape 对比
- [ ] **38 GEMM 极限冲刺**：wgmma+TMA+persistent 叠加，目标 ≥ 80% 同口径 cuBLAS（4096³ bf16）
- [~] **39 MLA / attention 极限冲刺**：叠加前述技巧，对标 FlashMLA，给出差距百分比。**已做（文章 20/21）**：SW128 swizzle 105.3 → 修 V 转置访存合并 142.3 → `cp.async` 单缓冲预取 K **159.8/193.2/198.8（Sk=1k/4k/8k）**，Sk≥4096 反超 `f4s`，距 FlashMLA ~640 约 **3.3×**；剩 TMA/双缓冲/warp specialization/消 QK dup，目标 250+（差距 ≤2.6×）。**（三）已试并受阻**：见「阻塞」——MN-major 免转置被硬件否决；跨迭代 QK/PV 软流水触发 ptxas `C7515` 串行化 + 255 regs/536B spill（147 vs 198.8）；1 CTA/SM 的 smem 几何（216KB）锁死延迟隐藏。下一步需要「生产/消费 warp 专门化 + V 双缓冲」或换几何，见「下一步」

### 第七部分：更远

- [ ] **40 nsys 端到端与 compute/comm overlap**（结合 `DeepEP` 概念）
- [ ] **41 Triton / TileLang 对照实现**：同一算子多 DSL 对比（呼应 flash-attention 05）
- [ ] **42 H100 vs A100 调优差异**：smem/TC/带宽对策略的影响

## 每篇的 Definition of Done

- [ ] 文章 `content/posts/cuda-kernel-opt-NN-<slug>/index.md`，`draft: false`，含 `weight`（=NN）
- [ ] 配套代码 `code/kernel-opt/NN-<slug>/`，实测通过，原始输出留存（`*.out.txt` / `.log`）
- [ ] 文章含：背景 → 现象/数据 → 根因 → 优化 → **实测对比表** → 小结 → 下一篇预告；模型类文章要给出真实 shape 与来源
- [ ] 关键结论有 ncu/实测支撑；引用的行号/API/shape 核对过
- [ ] **`TECHNIQUES.md` 至少新增 1 条技巧 + 更新性能榜**（记录手段、原理、代码位置、实测收益、坑）
- [ ] README.md 与 code/README.md 索引更新；ROADMAP 状态与「下一步」更新
- [ ] `hugo --gc --minify` 无 ERROR；publish 技能验证线上 200

## 工作循环（agent 自驱）

1. 读本文「下一步」，选一篇；若「下一步」为空，**自己从第五/六/七部分或 backlog 里补充具体、可测的新任务**再继续。
2. 写 / 改代码到 `code/kernel-opt/NN-*/`，用 `scripts/run.sh` 跑通，`scripts/ncu.sh` 采集关键指标。
3. 写文章，把实测数字填进去（不要编）；对标 SOTA 并给出百分比。
4. 更新 `TECHNIQUES.md`（技巧 + 性能榜）。
5. 走 `agent_skills/publish.md`：构建 → commit → push → 验证线上 200。
6. 更新本文件、README/code 索引、`agent_skills/kernel-opt.md` 踩坑。
7. commit & push（每篇一次）。

## 无人值守（autopilot）

`scripts/autopilot.sh` 循环启动 opencode 无头会话，每轮独立完成一篇文章增量并自动提交推送。
启动/查看/停止：`scripts/autopilot.sh {start|status|stop}`；日志 `autopilot.log`。

**重要：agent 永远不要自己创建 `AUTOPILOT_STOP`**——只在用户执行 `autopilot.sh stop` 时才停。
「下一步」做完了就自己扩充路线图，持续把算子性能往极致推。连续失败 3 次会由循环自动停。

## 阻塞

- **DeepGEMM 无法编译**（25 篇遇到，非硬阻塞）：`~/github/DeepGEMM` 的 `_C` 扩展依赖 `libdwfl`（`elfutils`/`libdwfl-dev`），`kernel_lab` 容器里没有，`import deep_gemm` 报 `_C` 缺失。已退化为「自研 per-expert loop + cuBLAS（`torch._scaled_mm`，eager + CUDA graph）对照」，并在文章里注明。若用户能补容器包（`apt-get install libdwfl-dev` 或等价），后续文章可给出与 DeepGEMM 的精确 grouped GEMM 差距。
- **MLA 极限冲刺（三）受阻（32 篇同轮调查，非硬阻塞）**：想从 198.8 TFLOPS 推向 250+，三条路都没走通——
  1) **MN-major 免转置不可行（硬件级）**：Hopper `wgmma` 的 B 操作数只按 K-major 解释描述符，`leading_byte_offset` 在 swizzle 布局下被忽略（改 LBO 结果逐位不变）。故 PV 的 V 转置不可避免。等 Blackwell `tcgen05` 或走 `mma.sync+ldmatrix.x4.trans`。
  2) **跨迭代 QK/PV 软流水被 ptxas 否决**：`mla_v3.cu` 里让 S 累积器在 softmax 消费后复用给下一块 QK，ptxas 报 `C7515`（非 wgmma 指令定义了 wgmma 累积器）主动串行化，且寄存器顶到 255 + 536B spill → 147 TFLOPS。即使用 wgmma predicate 覆盖代替清零、把 softmax 写独立 `E` 数组也无效（O 的 rescale 是 PV 累积器，跨迭代必然触发）。
  3) **V 装载改不了异步**：V 要「转置 + 8 行 gather」，TMA 无法转置、`cp.async` 无法散布；换成「16B 向量化 global 读 + 8 次标量 smem 写」反而 52.8（smem 散布 8×）；`prefetch.global.L1`/`__ldg` 均无收益。
  根因是 **216KB smem 锁死 1 CTA/SM**（Q 73.7K + K 73.7K + P 8K + V 65.5K），warp 数只有 8、延迟藏不住；而 KT 必须 ≥64（SW128 atom），BM=32 又不满足 wgmma 的 m64，几何上腾不出双缓冲空间。下一步靠 **producer warp 专门化**（见「下一步」文章 33）；若仍不行则先转做 MoE。

## 当前进度

- 2026-09-22：完成并发布 **文章 63（主题 26r）K2 的权重在显存里其实是连续的 —— 用 TMA 1D bulk 搬整 warp row-tile，把 FP4 decode FFN 推到 90% 权重 roofline**：接 62 的 ncu 判词（K2 `occ 34.3%`、`long_scoreboard 40.5%`、`NITER=K/1024=3`，`RWW/DEPTH/PIPE` 扫参全负 → 得改结构）。①**关键观察**：一个 warp 负责的 `RWW` 行在行主序下**连续**（`RWW*(K/2)` 字节），scale 段同理 → 用**一条 1D `cp.async.bulk`**（+1 条 scale bulk）搬进 smem，配 warp 私有 `mbarrier`（`count=1`，lane0 `arrive.expect_tx` 后发 2 条 bulk，其余 lane `try_wait.parity`）；每 warp 搬运指令从 `DEPTH*RWW=16` 条 `cp.async` 降到 2 条 bulk。②**同二进制端到端 4.9792→4.4874 ms（1.110×）**，权重 roofline **81.0%→90.0%**；K1 **3.2185→2.9230（83.3→91.7%）**、K2 **1.7291→1.5241（77.5→87.9%）**；B=128 **5.644→4.831（1.168×）**、B=256 6.893→6.464（1.067×）；K2 单算子同进程 head-to-head **1.7182→1.5066 ms（78.0→88.9%，1.14×，逐元素 max_rel=0）**。③**TMA 先发、激活后搬**（`EK` 位控）让 K1 再 +6.8%（85.7→92.0% HBM；tile 57KB 大、激活也大，DMA 飞行期间有活干），K2（tile 12KB、激活 3KB）反而 −1%，端到端取组合 `EK=3`。④**ncu 墙从 staging/延迟挪到 DRAM**：K1 **DRAM 92.24% / L2 92.41% / Compute 83.77% / occ 36.3%（72 regs、68.4KB smem）**；K2 **DRAM 90.35% / L2 90.90% / Compute 79.58% / occ 24.4%（64 regs、107.7KB smem → 2 CTA/SM）**——occ 比 62 的 34.3% 更低却更快，因为 TMA 把 latency 从 lane 的 dependency scoreboard 里拿出来（62 的 `long_scoreboard` 40.5% 消失）。⑤**负结果**：`L2::evict_first` hint 中性（sum 4.4721 vs 4.4651ms；L2 命中率仅 ~19%）；`DEPTH>NITER` 的 prologue 会越出行界 → illegal access（`issue(s)` 加 `min(s,NITER-1)` 兜底）；`RWW=16/32`（105/207KB smem）崩到 69.6%/18%；62 版在 mbar_init 后多加的一条 `__syncthreads` 值 ~3%（删掉即回）。⑥**判据**：搬运的是一段「连续、够大、行数固定」的字节 → 用 1D `cp.async.bulk`；TMA 的收益靠「飞行期间有活干」，tile/激活比决定「先发 TMA」是正还是负。⑦正确性全过：K1 GU 1.1e-7、swiglu <0.49×scale、K2 O 1.4e-7、unperm 7e-7、端到端 ~1e-7。代码 `code/kernel-opt/63-v4-fp4-ffn3/`（`fp4_ffn3.cu`、`k2_tma.cu`、`ffn_B64_128_256.out.txt`、`ffn_B64_baseline_pipe.out.txt`、`tsweep.out.txt`、`k2_tma_B64_headtohead.out.txt`、`k2_tma_sweep.out.txt`、`ncu_tma_k1k2.out.txt`、`ffn_B64_hint_negative.out.txt`）。**注意：下一篇候选是文章 64（主题 26s：把「连续行 → 1D bulk」推广到 34/30 篇的 prefill grouped GEMM；或 K1 的 L2 92.4% 墙——cluster multicast / 更大 row-span；或 B=256 K1 只有 63.7%，`MTMAX=4` 的激活 smem 把 occupancy 压到 2 CTA/SM，试激活分片）。**

- 2026-09-22：完成并发布 **文章 62（主题 26q）FP4 decode 的整 chunk 位拼装 —— 把每 nibble 一次 LDS 换成两条 `prmt`，K1 从 73.6% 到 83.4% HBM**：接 61 的 ncu 判词（K1 `Compute 75.9% ≈ DRAM 71.4%`，16 项 nibble 表的 LUT + `dp4a` 发射受限）。①**解码**：`prmt` 的 4-bit selector field **只用低 3 位、值 8–15 回绕到 `v&7`**（实测穷举发现）→ **原始 nibble 直接当 selector** 查 8 项幅值表（`T0=0x03020100,T1=0x0C080604`），再由 nibble 的 bit3 造逐字节符号掩码（`((w>>3)&0x11111111)*5` → `prmt(0,~0,·)`）在"幅值/负值"两表（`T2/T3`）间选；一个 uint32（8 nibble）**零 LDS** 出 8 个 int8，**顺序与原 k 一致** → `dp4a` 配对一字不改。②**结果（同进程 head-to-head，B=64 PIPE1）**：K1 **3.641→3.213 ms（73.6→83.4% HBM）**、K2 **1.941→1.727 ms（69.0→77.6%）**、kernel sum **5.600→4.957 ms**、端到端 **5.620→4.968 ms（71.8→81.1% 权重 roofline，1.13×）**；B=128 6.499→5.644（1.15×）、B=256 7.644→6.893（1.11×）；DEC=0（256 项字节表）≈DEC=4（字节表 LDS 减半但随机索引撞 bank，互相抵消）。③**指令/ncu**：总指令 **2.531e9→2.063e9（−18.5%）**、**LSU 637M→109M（−83%）**、ALU 1.497e9→1.230e9；K1 从 `ALU 75.9%` 变 **DRAM 83.6% / L2 89.6% / L1 21.1% / Compute 72.9% / occ 46.2%（56 regs）**；K2 **DRAM 78.2% / L2 84.7% / occ 34.3%（62 regs）/ `long_scoreboard` 40.5%**。④**K2 并行粒度重扫全负**：`PIPE1` 稳赢 `PIPE0`，K1 仍 `RWW4 s2`、K2 仍 `RWW8 s2`；K2 `NITER=K/1024=3` 太短 + occ 34% → 纯访存延迟受限，调 tile 补不上（下一步 TMA 整 row-tile）。⑤**判据**：`PRMT 换 LUT` 的胜负取决于墙在哪一级——58（并行度不足）输 29%、59/60（L1TEX 83.9%）输 33%、本篇 K1（ALU 75.9%、L1 71.8%）**赢 13%**。⑥正确性：K1 GU 1.1e-7、swiglu <0.49×scale、K2 1.4e-7、端到端 ~1e-7。⑦**坑**：`prmt` selector 按 4-bit field 从 LSB 排（不是按字节），按字节理解会静默错位；验证必须穷举。代码 `code/kernel-opt/62-v4-fp4-ffn2/`（`fp4_ffn2.cu`、`dec_test.cu`、`probe.cu`、`ffn_B64_dec{0,4,8}.out.txt`、`compare_B128_256.out.txt`、`sweep_B64.out.txt`、`ncu_k1_dec8.out.txt`、`ncu_k2_dec8.out.txt`、`inst_cmp.out.txt`）。**注意：下一篇候选是文章 63（主题 26r：K2 的 TMA row-tile）。**

- 2026-09-22：完成并发布 **文章 61（主题 26p）FP4 decode 的 MoE expert FFN 端到端 —— w1/w3→SwiGLU→w2，以及 cp.async 到底救不救得了延迟墙**：接 59/60 的单矩阵 grouped GEMV，扩到整条 expert FFN。shape 取自 `/ssd/models/DeepSeek-V4-Pro/config.json`（hidden=7168, moe_inter=3072, 384 routed top-6, expert_dtype=fp4），语义取自 `inference/model.py:596`（gate=w1、up=w3、clamp 10、`silu(gate)*up`）。①**五段流水**：`quant_x` → K1（合并 `[w1;w3] [2I,H]` grouped GEMV）→ `swiglu+per-128 int8 量化` → K2（`w2 [H,I]`）→ 加权反置换；全部复用 59/60 的 `gemv_fp4`（新增 `GATHER`/`PIPE` 模板 + 参数化 K）。②**关键语义 bug**：K1 激活 per-token（按 `toks[]` gather），K2 激活 per-(token,expert) 对（**不能** gather）；错用 gather 时 K2 `max_rel≈3.3`，且该 bug **只在 B≥128 暴露**（B=64 时 grouped 行号与原 pair 号巧合相等）。③**几何 sweep**（MTMAX=4）：K1 选 `RWW4 s2 PIPE1`（3.93ms），K2 选 `RWW8 s2 PIPE1`（2.27ms）；`cp.async` 双缓冲比寄存器环小赚 3~5%，**DEPTH 3/4 全线更慢**，寄存器环在 `MTMAX=1` 反常退化。④**MTMAX 随 B/64 分派**（1/2/4）→ B=64 端到端 +5%。⑤**权重 scale 换真实 E8M0 uint8 + `__int_as_float(e<<23)` 位技巧解码**：权重 15.85→13.48 GB（−15%），K1 只快 5.5% → 小 M GEMV 不在纯带宽墙（K1 ncu DRAM 71.4% ≈ Compute 75.9%）。⑥**结果（balanced，384 专家铺满显存 13.48 GB）**：B=64 **5.97 ms / 67.6% 权重 roofline**（PIPE0 7.91 ms），B=128/256 = 6.48/7.64 ms；K1 3.76 ms（2391 GB/s，71.3% HBM）、K2 2.18 ms（2064 GB/s，61.6%）、swiglu 8.5 µs。⑦ncu：K1 **DRAM 71.4% / Compute 75.9% / occ 36.4%（72 regs）**（LUT+`dp4a` 发射受限）；K2 **DRAM 62.0% / occ 23.5%（96 regs） / `long_scoreboard` ~35%**（延迟受限，无一级到顶）。⑧正确性全过：K1 GU 1.1e-7、swiglu 组内 <0.49×scale、K2 1.4e-7、端到端 **~1e-7**。代码 `code/kernel-opt/61-v4-fp4-ffn/`（`fp4_ffn.cu`、`ffn_B{64,128,256}_pipe{0,1}.out.txt`、`sweep_uint8.out.txt`、`ncu_k1_B64.out.txt`、`ncu_k2_B64.out.txt`、`ncu_swi_B64.out.txt`）。**注意：下一篇候选是文章 62（主题 26q：K1 的整 16B chunk 位拼装解码 / 按路由复用改 K2 并行粒度）。**

- 2026-09-22：完成并发布 **文章 60（主题 26o）FP4 grouped decode 的 LUT 真的是墙吗 —— 244.6M 次 bank conflict 被消掉，只快了 2.9%**：接 59 的判词「残余 48% 多余 wavefront 来自 256 项 `uint16` LUT 的随机读，下一堵墙是免冲突 LUT」。①**先数清楚**（`ncu_grouped2_M0`）：`shared_ld` **181.7M**、wavefront **475.8M（2.62/指令）**、bank conflict **244.6M（1.35/指令）**；LUT 读 = 每 weight byte 一次 = **5.3e9 lane-read**，是 L1/TEX 83.4% 的绝对主因。②**LUT 布局消融**（同 kernel 只切 `LUTMODE`）：`uint32[256]`（0.93×）、`uint16`×2/×4 副本 + lane 选副本（0.95/0.91×）**全部更慢**——随机索引命中某 bank 的概率恒为 1/32，换分布不消冲突；只有 **16 项 nibble 表**（索引空间 16≤32、每项独占 bank）结构上无冲突。③**M4 机制大胜、墙钟小胜**：conflict **244.6M→0.12M（÷1993）**、`short_scoreboard` **3.01→0.64（÷4.7）**、L1/TEX **83.4→68.8%**，但耗时 **2.6177→2.5467 ms（+2.9%）**、DRAM 只 60.4→62.0% → **瓶颈是 DRAM 延迟，不是 L1 那一级吞吐**（代价：每 byte 两次 LDS，指令 181.7M→313.8M）。④**occupancy 锁死**：regs 119/124 → **2 CTA/SM**（`acc[4][4]`+ring+`x8[4][8]` ≥88），`__launch_bounds__(256,3/4)` 溢出（2.95/5.09ms）、`MTMAX=8` 溢 884B（3.60ms）、几何/深度扫全负（RWW4 D1 最佳）。⑤**真实稀疏路由是模型层真杠杆**：按活跃专家压缩网格（`grid.y=n_active`），B=64 幂律 active=168/384 → **1.414 ms**（读 44% 权重，相对 balanced **2.086×**）、hot-75% 在 B=64/128/256 = **1.30×/1.26×/1.24×**；但有效带宽随网格缩小下滑（60%→44%）。⑥正确性 `max_rel ~1.2e-7`；**M4 的收益随 m 出现**（B=64 m=1 时 **−1.3%**、B=256 m=4 +2.9%）。代码 `code/kernel-opt/60-v4-fp4-grouped2/`（`fp4_grouped2.cu`、`grouped2_B{64,128,256}.out.txt`、`ncu_grouped2_M{0,4}.out.txt`、`ncu_grouped2_M4_bytes.out.txt`）。**注意：下一篇候选是文章 61（主题 26p：把 grouped GEMV 接进 decode MoE FFN 端到端 `w1/w3→SwiGLU→w2`）或 26q（`cp.async` 深流水把权重搬进 smem，解耦 global 延迟）。**

- 2026-09-22：完成并发布 **文章 59（主题 26n）FP4 decode 的 grouped GEMV —— 把 top-6 的 6B 个 `(token,expert)` 对按专家折叠，权重只读一遍**：接 58 的判决「单专家 M=1 受 `N=3072` 并行度上限」。shape 取自 `/ssd/models/DeepSeek-V4-Pro/config.json`（`hidden=7168, moe_inter=3072, 384 routed, topk=6, expert_dtype=fp4`）；一步 decode（batch B）要算 `6B` 个 `(token,expert)` 对，权重 FP4+E8M0 = 13.76 MB/专家，384 专家 = **5.28 GB**。①**结构**：一个模板 kernel 三口径——`naive_loop`（58 的 pipe kernel 逐对 launch）、`batched_m1`（一次 launch、grid.y=pairs、M=1）、`grouped`（一个 kernel、`grid.y=384 experts`、一个 CTA 吃下某专家全部 m 个 token，**权重每专家只读一次/decode 一次、被 m 个 token 的 `dp4a` 复用**）；权重复用 `=pairs/active≈B/64`，算术强度 `AI≈3.76·m`。②**两修复**：**smem/寄存器随真实 m 缩放**（`MTMAX` 按 `B/64` 分派模板 + 每实例 `cudaFuncSetAttribute`）+ **两平面激活布局**（chunk 的前/后 16B 分进 plane0/plane1，lane 地址由差 32B 变差 16B）消掉激活 smem 的 **8-way bank conflict**；ncu 多余 wavefront 54%→48%、batched 基线 67.6%→**74.5%**、grouped RWW2 40.6%→49.9%。③**主结果（平衡路由 `(t*6+j)%384`，每专家恰好 m=B/64 个 token、零 padding）**：B=256（m=4,RWW=4）**2.63 ms / 有效权重带宽 2008 GB/s / 59.9% HBM**（naive 15.44ms→**5.87×**、batched_m1 8.47ms→**3.22×**）；B=64（m=1,RWW=2）**82.7%**、B=128（RWW=8）74.3%、B=512（RWW=2）35.7%（复用越大绝对时间越省：单 token 29.8→10.3→8.6 µs）；roofline 全专家权重 1.58ms 的 **~60%**。④**ncu（B=256,RWW=4）**：**DRAM 60.1% / L1/TEX 83.9%** / L2 63.7% / Compute 41.1% / regs 117 / occ 24.4% / short_scoreboard 3.0 → **墙从 DRAM 挪到 L1/TEX**，残余冲突来自 LUT 随机读。⑤**负结果**：PRMT 换 LUT **慢 33%**（3.50 vs 2.63ms，延续 58「换访存税为 ALU 税方向反了」）。⑥正确性 `max_rel ~1.1e-7`（权重/scale 按 expert 扰动，能抓索引错）。代码 `code/kernel-opt/59-v4-fp4-grouped/`（`fp4_grouped.cu`、`grouped_B{64,128,256,512}.out.txt`、`ncu_grouped_g4.out.txt`、`ncu_batched_m1.out.txt`）。**注意：下一篇候选是文章 60（主题 26o：冲突无关 LUT / 稀疏路由）或文章 26l（TMA + host 置换布局推广）。**

- 2026-09-22：完成并发布 **文章 58（主题 26m）FP4 decode GEMV 的第二轮 —— 从 34.8% 到 38.7%，以及为什么 split-K / 无 smem / 位运算解码都救不了它**：接 56 的尾巴「免冲突 LUT / IMMA 把 FP4 decode 从 35% 推向 70%+」。shape 取自 `/ssd/models/DeepSeek-V4-Pro` 真实 checkpoint 的 `w1 [N=3072, K=7168]`（FP4 e2m1 + E8M0 block-32，11 MB；M=1/2/4）。①**先诊断**：56 的 kernel `Waves Per SM = 0.36~0.48`、occ 32.8%、`Issued Ipc Active 1.08`、Issue Slots Busy 20.9% → **并行度不足**（一行一 warp ⇒ `#warp=N/RWW=3072`、23/SM），不是 bank conflict；②**唯一有效**：把权重预取深度从 1 调到 2（环形 `uint4`）→ **0.0100 → 0.0090 ms / 1296.6 GB/s / 38.7% HBM（1.11×）**，`long_scoreboard` 5.43→5.05；③**全部负结果**：**split-K**（KS=1→8 单调 0.0091→0.0118，即使激活只载 k-slice）、**位运算 PRMT 解码**（用 `__byte_perm` + 8 项常量表，全正确 `bad=0` 但 0.0117/29.9%——ALU 化）、**无 smem + PRMT + KS=8**（0.0127）、**全量预取**（0.0096，寄存器反噬）、**RWW=2**（0.0098）；④**LUT vs PRMT 取决于 M**：M=1 LUT 0.0090 < PRMT 0.0117；M=4 **PRMT 0.0133 < LUT 0.0140**（解码被 4 token 复用）；⑤距 roof（0.0041ms/86.1%）**2.2×**，判决=**形状（N 小）决定天花板**，只能靠 grouped（多专家合并）改并行粒度，单点指令优化补不了延迟墙。代码 `code/kernel-opt/58-v4-fp4-decode/`（`fp4_decode.cu`、`prmt_test.cu`、`decode_M{1,2,4}.out.txt`、`ncu_v5_pipe_d2.txt`）。**注意：下一篇候选是文章 59（主题 26n：把 FP4 decode 接成 grouped GEMV——多专家合并 + 更高并行度）或文章 59（主题 21i：host 预排布 FP4 + chunk-local 折叠兑现 prefill 1.26× 地板）。**

- 2026-09-22：完成并发布 **文章 57（主题 21h）FP4 专家权重接回 grouped MoE —— 现场折 vs 预折叠，与一份被「变换税」吃掉的字节红利**：接 56 的尾巴「把 FP4 无损折叠后的权重接回 34 篇 grouped kernel 量 prefill 端到端」。shape 取自 `/ssd/models/DeepSeek-V4-Pro/config.json`（`hidden=7168, moe_inter=3072, N=2I=6144`，384 exp top-6；真实权重 **FP4 e2m1 + E8M0 block-32** vs 预折叠 fp8）。①**一条 kernel 两个模式**：MODE0 预折叠 fp8 / MODE1 现场折 fp4，consumer（wgmma `m64n128k32` + per-128 `sa` 折算）逐字节相同；B 走 cp.async + **手写 SW128**（fp8 一行 128 个元素、16B chunk 向量化置换），现场折用 **4096 项 `(exp,nibble)->e4m3` smem LUT**（16B packed = 32 个 fp4 = 1 个 block-32 scale，整块共用一个 exp）。②**正确性**：现场折与预折叠 **逐位相等**（`max_abs_diff=0.000e+00 bad=0`）+ CPU 参考 199/200。③**主结果（M=8192 balanced，零 padding）**：BN=256 预折叠 **7.76 ms / 557.8 TFLOPS（28.2% FP8 峰值，2179 GB/s fp8 = 65% HBM）** vs 现场折 **11.69 ms / 370.4 TFLOPS（769 GB/s fp4）**——**现场折慢 1.51×**（BN=128 时 1.44×），与「字节减半应更快」完全相反。④**根因 ncu**：两者都 **Compute (SM) 95.3% / DRAM 47%**（等发射口 39.8% + 未选中 39.7%）→ 时间花在**变换指令**不是等 HBM。⑤**诊断（DIAG 短路 swz_stage）**：地板 FP4 **5.25 ms** vs FP8 **6.60 ms**（**1.26×**，字节红利真实）——但 MODE1 折+置换税 **6.44 ms**（MODE0 仅置换 1.16 ms）**> 省下的 ~2.4 ms**。⑥**判据** $C_\text{tr} < \Delta B/W = 7.93\text{GB}/3.35\text{TB/s} = 2.37\text{ms}$，实测 6.4 ms（差 2.7×）→ **prefill 用 host 预折叠存 FP8 更划算，decode（带宽敏感）才轮到现场折**；本质是 **FP4 迫使你放弃 TMA**（TMA 的 SW128 是硬件免费做的）。⑦坑复现：helper kernel 忘写 grid-stride 只初始化 0.04% 权重（且 CPU 参考读同一份坏数据 → 对拍全过、藏 bug 一整轮）、host `float(fp8)`/`(unsigned char)fp8` 都不是读原始字节、SW128 fp8 chunk 下标是 `k/16` 不是 `k/8`、wgmma 单 warpgroup 只算 m64。代码 `code/kernel-opt/57-v4-fp4-moe-fold/`（`fold_moe.cu`、`swz_test.cu`、`gemm_smoke.cu`、`fold_M8192_bal_BN{128,256}.out.txt`、`fold_M8192_bal_BN256_diag.out.txt`、`ncu_pf_BN256.out.txt`、`ncu_fp4_BN256.out.txt`）。**注意：下一篇候选是文章 58（主题 21i：host 预排布 FP4 + chunk-local 折叠，把变换税压进 2.4 ms 兑现 1.26× 地板）或文章 58（主题 26m：免冲突 LUT/IMMA 把 FP4 decode 从 35% 推向 70%+）。**

- 2026-09-22：完成并发布 **文章 56（主题 21g）DeepSeek-V4 的路由专家其实是 FP4——真实 checkpoint 的格式、无损折进 FP8、与 decode GEMV 的实测**：本来要接 55 的尾巴做「真正的 per-128×128 block 量化器」，结果先读了一遍真实 checkpoint 的 tensor 头，发现 34 篇（及 31/36/37）一直默认的「专家权重 = FP8」是**错的**——`config.json` 的 `quantization_config` 只描述默认方案，被 per-tensor 的 **`expert_dtype: fp4`** 覆盖。①**格式**（读 `/ssd/models/DeepSeek-V4-Pro` layer0 expert0 实测）：routed experts `w1/w3 [3072,7168]`、`w2 [7168,3072]` 存成 **FP4 e2m1 + E8M0 block-32**（`int8[.,./2] + E8M0[.,./32]`，scale 全是 2 的幂、`log2` 偏差 **0.0**）；shared expert / 注意力仍是 FP8 e4m3 block-128；lm_head bf16。②**无损折叠**：`e2m1 ⊂ e4m3` 且 scale 是 2 的幂 → FP4 折成 e4m3 **逐位无损（relRMS = 0.00e+00，真实权重实测）**；专家权重 bf16 **3095 GB** → fp8 1547 GB → **fp4 773.7 GB（+scale 48.4 GB）**；34 篇的 fp8 grouped kernel 只需 host 侧一次无损预折叠即可原样消费。③**decode GEMV**（真实 w1 fp4，M=1）：算术解码是 **compute-bound**（ncu Compute **65.5%**、DRAM 19.6%、0.0199ms/17.5%）；换 **256×uint16 smem LUT**（一 byte→两 int8）→ **0.0100 ms / 1165.6 GB/s（34.8%）**、**1.99×**，Compute 降到 20.5%；同 shape **fp8 权重路径 0.0216ms** → FP4 **2.16×**。④**下一堵墙**：LUT 随机 `uint16` 读撞 bank（59% 多余 shared wavefront、`short_scoreboard` 4.54、`long_scoreboard` 5.96）→ 免冲突 LUT / IMMA / 把解码摊到 prefill（权重被 $B_M$ token 复用，解码税趋零）。⑤正确性 vs CPU 参考 max_rel 9.4e-8。代码 `code/kernel-opt/56-v4-fp4-moe/`（`extract.py`、`fp4_gemv.cu`、`real_weight_stats.out.txt`、`fp4_gemv_M1.out.txt`、`ncu_lut.out.txt`、`ncu_arith.out.txt`）。**注意：下一篇候选是文章 57（主题 21h：把 FP4 无损折叠后的权重接回 34 篇 grouped kernel 量 prefill 端到端）或文章 57（主题 26m：免冲突 LUT/IMMA 把 FP4 decode 从 35% 推向 70%+）。**

- 2026-09-22：完成并发布 **文章 55（主题 28b）FP8 融合 cross-entropy / LM head：当权重减半，那个卡在 L2 上的 GEMM 会变快吗**：接文章 54 的尾巴「把 V4 的 FP8 LM head 用 FP8 wgmma 做」。shape 取自 `/ssd/models/DeepSeek-V4-Pro/config.json`（`hidden_size=7168`、`vocab_size=129280`、`e4m3+ue8m0+weight_block 128×128`），`M=2048/8192/32768`。①**实现**：把 54 的 `fce_kernel` 用 `bool FP8` 参数化——SW128 的 128B atom 对 bf16（8×64）与 fp8（8×128）**逐字节同构**，只差 `BK`（64/128）、TMA `rowbytes`（`K*2`/`K`）、wgmma 指令（`m64n128k16` vs `m64n128k32.e4m3`）；epilogue 把 **per-row 激活 scale（2 的幂、精确）+ per-tensor 权重 scale** 乘回（`acc[j*4+0/1]*=s0; ...*=s1`，同行 4 累加器共用 `s0`），再走 54 的 tile 级 online logsumexp + superblock swizzle。②**主结果**：`sw 8×16` **12.82 ms / 1184.6 TFLOPS（59.9% FP8 峰值）**，**同进程 bf16 融合（25.01 ms / 607.1）的 1.95×**、cuBLAS bf16+CE（24.67 ms）**1.93×**、`cuBLAS FP8 GEMM+CE`（13.80 ms，torch `_scaled_mm`）**1.08×**，仅比 cuBLAS FP8 GEMM 单独跑（11.97 ms / 1268.1）慢 7%（还顺手做了 CE）；跨 M 1.75×/1.93×/1.81×。③**瓶颈翻转**：ncu DRAM **24.0%→11.5%**，L2 79.0% + tensor 73.4% 双限（bf16 是 L2 82.9% + tensor 78.8%）——**「融合 CE 不赚吞吐」的判决在 FP8 下反转**（张量核峰值翻倍）。④**精度**：logits fp8 vs bf16 rel-RMS **3.7%**、max-abs 3.9e-3，但 **CE loss 只差 1e-5**（11.769796→11.769786）；自家 bf16 kernel vs cuBLAS bf16 **逐位一致**。⑤负结果：`256×256`（C7511，7.6%）、`STAGES=5` smem 245KB>232KB（`cudaFuncSetAttribute` 失败 → 扫参出现 **10^7 TFLOPS 假数据**，教训=扫参必须查 attribute 返回值）。⑥边界：真实 checkpoint 的 lm_head 是 bf16（`inference/model.py:712`），per-128×128 block 可无损折进操作数（37 篇）→ 本吞吐数字对 per-block 原样适用，只有 host 量化器要换。代码 `code/kernel-opt/55-fp8-fused-ce/`（`fused_ce_fp8.cu`、`torch_fp8_ref.py`、`sweep_M{2048,8192,32768}.out.txt`、`acc_M8192.out.txt`、`torch_fp8_ref.out.txt`、`ncu_fp8_sw8x16.out.txt`、`ncu_bf16_sw4x8.out.txt`）。**注意：下一篇候选是文章 56（主题 28c：把 lm_head 换成真正的 per-128×128 block 量化器 + 在真实 outlier 权重上量精度，或直接去攻 79% 的 L2——cluster multicast / 更大面板）或文章 49（主题 25d MLA decode WS，暂缓）。**

- 2026-09-22：完成并发布 **文章 54（主题 28）融合 cross-entropy / LM head：把 2.12 GB 的 logits 从显存里省掉，和那个卡在 L2 上的 GEMM**：shape 取自 `/ssd/models/DeepSeek-V4-Pro/config.json`（`hidden_size=7168`、`vocab_size=129280`），`M=8192`，logits `[8192,129280]`（bf16 2.12 GB / fp32 4.24 GB）。①**融合设计**：把 `lm_head` GEMM（TMA+wgmma SS SW128，复用 35 篇骨架）与 CE 融成「**split-N**（沿词表切，不切 K → 免 split-K 归约税）+ **tile 级 online logsumexp** + 跨 tile 合并」；`acc[64]` 每行由 4 个 `tig=lane&3` 线程持有，行 max/Σexp **全在 warp 内**（2 次 `shfl_xor`），写 `partial_max/sum[m][T]`（T=V/BN，66 MB）+ target logit 由命中 tile 的线程就地写。②**关键工程点=superblock L2 swizzle**（`GM×GN` 面板住 L2、相邻 CTA 共享 A/W tile）：DRAM 流量 **60.21 → 17.72 GB（3.4×）**，ncu 时长 25.56→22.04 ms，瓶颈从 DRAM 70% 搬到 **L2 82% + tensor 79%**。③**结果**：`sw 4×8` **25.07 ms / 605.7 TFLOPS（61.2% bf16 峰值）**，vs cuBLAS+CE（20.33+1.90=**24.53 ms**）**0.98×**、vs PyTorch eager（**23.04 ms**）**0.92×**、vs fp32-logits 基线（~29.0 ms）**1.16×**；logits 显存 → **66 MB 部分和**；loss 与 cuBLAS+CE 6 位小数一致（11.76979x）。④**判据（本篇最重要）**：logits 往返流量 $\propto MV$、GEMM 流量 $\propto MVK(1/B_M+1/B_N)$，比值 $=2/[K(1/B_M+1/B_N)]$，本 shape 仅 **2.4%** → **大词表 compute-bound LM head 上，融合 CE 是显存优化而不是吞吐优化**（Liger 的动机也是显存）。⑤**负结果**：`256×256` 触发 ptxas `C7511` wgmma 串行化崩到 **75.9 TFLOPS**；`128×128` 想换 2 CTA/SM 但 B 重读翻倍仅 34.5%；浅 stage −5~13%；epilogue **免费**（融合≈只写 logits）。⑥**坑**：swizzle 的块内解码在 `Nt/GN` 不整除（1010/8）时会**漏算 tile**、`partial` 留脏值 → loss 悄错（11.764 vs 11.7698），改成「1D 网格 `ceil(Mt/GM)·ceil(Nt/GN)·GM·GN` + 越界 return」才严格双射；**必须用 loss 对拍验证**。另：本轮多个 `timeout` 留下的**僵尸 `fused_ce.out` 死锁进程持续占着 GPU 0**，让所有基准虚慢 ~2×（且宿主 pkill 报 stack 未清），是「跑基准前先确认 GPU 干净」的又一次复现。代码 `code/kernel-opt/54-fused-ce/`（`fused_ce.cu`、`torch_eager.py`、`final_sweep.out.txt`、`torch_eager.out.txt`、`ncu_fused_sw4x8.out.txt`、`ncu_fused_nosw.txt`、`ncu_plain_sw4x8.txt`）。**注意：下一篇候选是文章 55（主题 26l：把「TMA + host 置换布局」推广到别的低比特算子）或文章 56（把 V4 的 FP8 LM head 用 FP8 wgmma 做融合 CE，权重字节减半）。**

- 2026-09-22：完成并发布 **文章 47（主题 26g）给 W4A16 小 M 的 `mma_b16` 叠 warp specialization：一个负结果，和真正的墙（反量化指令 + KSPLIT 并行度）**：shape 取自 `/ssd/models/qwen3-8B/config.json`（`hidden=5120, intermediate=17408, group=128`，对称 int4），`N=17408, K=5120`。①**先量墙**：`mma_b16_k8` M=16 的 ncu——tensor pipe 仅 **7.3%**、`pipe_tensor` 0.70M（2.3%），而反量化的 `pipe_alu` 14.5M + `pipe_fma` 9.4M（**78%**）；DRAM 21.5%、`long_scoreboard` 1.59 → **指令发射 + 访存延迟受限，不是张量核受限**。②**WS 设计**：把 46 的 `gemm_mma_w4a16_kernel` 拆成「1 producer WG `cp.async`+int4→bf16 → `Bs`」+「NCONS consumer WG 只 `ldmatrix`+`mma`」，`mbarrier` full/empty（23/41 协议），与 46 逐位等价（err 2.6e-3）。③**WS 是彻底的负结果**：M=16 最好的 `ws_b16_s2c2k8` **0.0771ms** 仍慢于 `mma_b16_k8` 0.0752，其余 0.090–0.101（`s2c1k8` 0.1006）；ncu 证明被拆走的反量化**指令一条没少**（ALU 15.0M、FMA 11.3M、tensor 0.696M 全一样），总指令 **+9%**，且 `long_scoreboard` **1.77→10.19（5.8×）**——生产者 warp 独自干等。根因：**WS 只能重叠、不能减少工作**，而反量化是「要等结果」的访存密集活，不是 TMA 式异步发射。④**真正的提升=KSPLIT 粒度**：k8→k16（waves 2.06→4.12）在 `M∈[3,16]` 一致 **+5~11%**（M=3 0.0683→0.0638、M=8 0.0717→0.0665、M=16 0.0752→0.0728），k16 甜点、k24/k32 被 prologue/drain+`atomicAdd` 归约税吃回（M=16 k32 比 k16 慢 13%）。⑤**更宽的 `mma_b32_k16` 在 M=24 反超 wgmma**（0.0824 vs 0.0858），分派表细化为四段（`≤2` GEMV / `3–16` `mma_b16_k16` / `~17–25` `mma_b32_k16` / `≥26` `wgmma`）。⑥新自适应 12 点求和 **0.8252ms = always-wgmma 的 0.813（1.23×）**、always-GEMV 的 4.79×（46 篇为 0.840/1.19×）。⑦M=16 有效权重带宽 650 GB/s / 19.4% HBM，距同进程纯读上界（0.0182ms/2606GB/s/77.7%）**4.0×**——下一步必须减少反量化指令（W4A8+IMMA）。代码 `code/kernel-opt/47-w4a16-mma-ws/`（`w4a16_mma_ws.cu`、`sweep.out.txt`、`ncu_mma_b16k8_M16.out.txt`、`ncu_ws_b16_s2c2k8_M16.out.txt`、`ncu_mma_b16_k16_M16.out.txt`）。**注意：下一篇候选是文章 48（主题 26h：W4A8 整数张量核 IMMA 小 M GEMM）或文章 49（主题 25d：MLA decode WS，本轮评估后暂缓，见下）。**

- 2026-09-22：完成并发布 **文章 48（主题 26h）W4A8 的整数张量核（IMMA）：把反量化的 ALU 砍掉一半，小 M 权重带宽翻倍**：shape 取自 `/ssd/models/qwen3-8B/config.json`（`hidden=5120, intermediate=17408, group=128`，权重对称 int4，激活 int8 per-128 动态），`N=17408, K=5120`。承接 47 篇的判决「`mma_b16` 78% 的指令是反量化 ALU」——**换数制**。①先写 `imma_test.cu` 钉死 `mma.sync.m16n8k32.s32.s8.s8.s32` 片段布局（A 的 `a0`=「row lane>>2、col (lane&3)*4」=4 个连续 int8=1 个 word；**完全不用 `ldmatrix`**），单条对拍 **0/128 mismatch**；②`BK=128=GROUP`，一个 stage 一个 scale group，int32 累加、group 末尾 `facc += sa·sw·(acci − 8Σq_a)` 折算；③**两个 ALU 小招**：权重按 **uint16 直读** + `PRMT`（`expand4u`）一次摊 4 个 int8；`u8.s8`（B=nibble 无符号）省掉逐 byte `-8`，解析扣 `8Σq_a`（**类型序是 `.s8.u8`，A 在左**，写反不报错、rel 116× 只能靠对拍抓）；④**去掉 23KB smem scale 表**改 `__ldg`（+12%，寄存器反升 92→101、occupancy 31%→23% 仍更快，省的是 prologue）。⑤**结果**：M=16 **0.0376ms / 1290 GB/s / 38.5% HBM**（`imma_ns_s2k7`；bf16 `mma_b16_k16` 0.0722 → **1.92×**），M=1 0.0320 / 44.2%，全 M 段总和 **1.80×**（0.9804→0.5439ms）；M=24/32/48 `s2k5` 0.0521/0.0548/0.0735、M=64 `s2k4` 0.0926；单点最高 1.92×。⑥**ncu 根因**：指令 **30.6M→12.4M（−59%）**、ALU pipe **43%→34%**、DRAM **22.6%→42.1%**、Compute 47.7%→38.1%、occ 23%/101 regs/waves 2.06、`No Eligible` 56.8%、主导 stall 从平衡变成 **`long_scoreboard` 2.74**（bf16 1.58）→ **墙从 ALU 挪到访存延迟**。⑦**负结果**：`__launch_bounds__(128,7)` 强压 101→72 regs → **40B spill、慢 35%**（延迟不能靠堆 occupancy 填）。⑧**数值**：整数运算**精确**，相对误差 **1e-7**（bf16 3.6e-3）；唯一近似是激活 int8 量化本身（~0.4%）。⑨对标：相对 40 篇 Marlin 类口径（400 GB/s）**3.2×**，差距 6.7×→**~2.1×**；纯读上界（2568 GB/s）的 **50%**。代码 `code/kernel-opt/48-w4a8-imma/`（`w4a8_imma.cu`、`imma_test.cu`、`sweep_all.out.txt`、`imma_test.out.txt`、`ncu_imma_ns_s2k8_M16.out.txt`、`ncu_imma_ns_s2k8_stalls.out.txt`、`ncu_bf16_mma_b16_k8_M16.out.txt`、`ncu_imma_inst_M16.out.txt`、`ncu_imma_best_M16.out.txt`）。**注意：下一篇候选是文章 49（主题 25d：MLA decode 的 producer/consumer WS）或把 IMMA 与 43 篇持久化叠加继续压 `long_scoreboard`。**

- 2026-09-22：完成并发布 **文章 46（主题 26f）小 M decode 的 GEMV ↔ 张量核分派：交叉点不是 M≈78，而是 M≈2.5 / M≈17**：真实 shape 取自 `/ssd/models/qwen3-8B/config.json`（`hidden=5120, intermediate=17408, group=128`，对称 int4），`N=17408, K=5120`，扫 12 个 M 点（1/2/3/4/6/8/12/16/24/32/48/64）。①**证伪 44 篇的 AI 估算 $M^\*\approx78$**：两条路都没在各自 roofline 上——小 M 的 `wgmma.m64` 只有 16–18% HBM（延迟/occupancy 受限），时间几乎常数 0.083ms，而 GEMV 时间 $\propto\lceil M/MT\rceil$ 线性爬，真交叉点 **M≈2.5 / M≈17**。②**新写 `mma.sync.m16n8k16` 小 tile**（BM=16/32/64，`cp.async` + 主循环内 int4→bf16，B 存 `[N][K]` 用**非转置** `ldmatrix.x4` 取 B 片段）检验「小 BM 消除 63/64 行税」：初版 `waves/SM=0.26`、DRAM 10.3%（网格仅 136 CTA）、`mma_b32` M=16 **0.1447ms**；加 **`KSPLIT=8`** 后 waves 2.06、DRAM 22.6%、**0.0816ms（1.77×）**。③**最优由「权重读遍数 $P=\lceil M/d\rceil$」决定**：`M≤2` GEMV（`d=1`，0.0309/0.0379，M=1 46.0% HBM、ncu DRAM 47.4%/occ 55%/1.59TB/s）；`3≤M≤16` `mma_b16_k8`（`d=16`，每遍 0.070 < wgmma 0.083，0.0706–0.0750）；`M≥17` `wgmma.m64`（`d=64`，mma 要第 2 遍，0.084–0.096）。④**运行时分派器** 12 点求和 = always-wgmma 的 **0.838（1.19×）**、always-GEMV 的 0.215（4.63×）；always-mma 0.973 略胜 always-wgmma。⑤正确性：GEMV rel 1e-7、张量核 rel 2.6e-3（int4 量化）；纯读权重上界 0.0182ms/2598GB/s/77.5%。代码 `code/kernel-opt/46-w4a16-smallm/`（`w4a16_smallm.cu`、`sweep.out.txt`、`ncu_gemv_r2mt1_M1.out.txt`、`ncu_mma_b16k8_M16.out.txt`、`ncu_mma_b32_M16.out.txt`）。**注意：下一篇候选是文章 47（主题 25d：MLA decode 的 producer/consumer WS）或 48（主题 26g：给 `mma_b16` 叠 WS 把 22.6% HBM 推向 40%+）。**

- 2026-09-22：完成并发布 **文章 45（主题 26e）W4A8 + `dp4a`：把 M=1 decode 的反量化 ALU 税从 60% 打到 30%**：shape 取自 `/ssd/models/qwen3-8B/config.json`（`hidden=5120, intermediate=17408, group=128`，对称 int4），`M=1, N=17408, K=5120`。①**动机**：44 篇 ncu 判 ALU pipe 64.9% 是墙，反量化把每个 int4 变成 3 串 ALU 指令。②**W4A8 设计**：激活量化 int8（per-128 动态），内层用 `__dp4a` 做整数点积；权重 nibble 经 `PRMT`（`__byte_perm`）铺成 byte。③**v1 坑**：用 `__vsubss4` 逐 byte 做 `q=u-8`（且 SWAR 的 `(x^8)-8` 有跨 byte 借位全错）——`__vsubss4` 在 sm_90 被 ptxas 展开成大量 `LOP3/PRMT`，SASS 里 494 LOP3/240 PRMT，只到 **0.0226 ms / 62.4% HBM**、ALU 仍 60.1%。④**关键一步**：nibble 当**正整数**直接喂 `dp4a`（`Σ(q+8)q^x`），再解析扣掉 `8·Σq^x`（`Σq^x` 与行无关，每 chunk `__dp4a(ones,x)` 一次）→ `expand8u` 降到 5 指令，**指令数 11.87M→7.17M（−40%）**。⑤**结果**：最佳 `w4a8h_r4` **0.0185 ms / 有效权重带宽 2408.4 GB/s / 76.3% HBM**，相对 44 篇 W4A16（0.0283/49.8%）**1.53×**，越过同进程纯读上界（0.0190/74.4%）；ncu 从「ALU 60.1%」翻转为「**DRAM 66.7%、Compute 29.8%**」。⑥**激活量化 kernel**：一线程/group 只有 40 活跃线程 → 0.01084 ms（比 GEMV 还贵）；改「一 warp 一 group（lane 4 元素 + `shfl` 归约 amax）」→ **0.00253 ms（4.3×）**；端到端量化+GEMV 0.0210 ms = W4A16 的 **1.35×**。⑦**数值**：int8 激活 per-128 量化额外相对误差 **~0.4%**；W4A16 同权重参考 6e-8。⑧**小 M**：MT 共享权重，单 token 成本 0.0121(M2)/0.0066(M4) 在降但绝对时间涨 → `M≳64` 换回张量核。代码 `code/kernel-opt/45-w4a8-gemv/`（`w4a8_gemv.cu`、`sweep_M1.out.txt`、`bench_M2/M4.out.txt`、`ncu_b_r2.out.txt`、`ncu_h_r4.out.txt`、`ncu_inst_cmp.out.txt`）。**注意：下一篇候选是文章 46（主题 26f：`M∈[2,16]` GEMV↔GEMM 自适应）或文章 47（主题 25d：MLA decode WS）。**

- 2026-09-22：完成并发布 **文章 44（主题 26d）W4A16 的 M=1 GEMV：M=1 时张量核 63/64 的行在空转，不如回去做带宽最优的 GEMV**：shape 取自 `/ssd/models/qwen3-8B/config.json`（`hidden=5120, intermediate=17408, group=128`，对称 int4），`M=1, N=17408, K=5120`（权重 int4 44.6 MB + scale 2.8 MB）。①**先算 AI**：`AI(M)=2MNK/(0.53NK)≈3.76M`，bf16 TC ridge≈295 → 要到 `M≈78` 张量核才划算；40–43 的 `wgmma.m64n128k16` 在 M=1 白算 63/64 行，kernel 退化到延迟受限（DRAM 11–18%）。②**GEMV 设计**：每 warp 一行、warp 内 32×16B chunk 沿 K 并行（相邻 lane 相邻 16B，完全合并）、`x`(10KB)/scale 常驻 smem 供 `8×RWW` 行复用、5 步 `shfl_xor` 归约、`__ldcs` 流式读权重。③**结果**：最佳 `RWW=2` **0.0284 ms / 有效权重带宽 1566.6 GB/s / 49.7% HBM**，相对 43 篇 tensor-core M=1（0.0798，同进程亲手跑）**2.81×**、相对 naive（0.4583）**16.1×**；纯读上界 0.0187 ms/2380 GB/s/75.4%。④**ncu**：ALU pipe **64.9%**、FMA 27%、Compute 57.8%、DRAM 47.7%、occ 54.8% → **ALU 发射受限**，不是访存。⑤**负结果（都实测）**：免冲突置换布局（conflicts 210 万→5.4 K，但 **−6%**，因拆散了 16B 局部性）、smem LUT 反量化（16 项 −14%、256 项 `float2` **−40%**，随机 8B 读冲突）、`__int_as_float(0x4B000000|n)` 位技巧（f32 灾难性抵消，err 15% FAIL）、`Σ(q-8)x` 代数化简（−6%，新增串行依赖）。⑥**小 M**：`MT` token 共享权重，单 token 成本 0.0284→0.0176(M2)→0.0148(M4)，但绝对时间随 M 涨 → `M≳64` 该换回张量核 GEMM。代码 `code/kernel-opt/44-w4a16-gemv/`（`w4a16_gemv.cu`、`gemv_M1.out.txt`、`gemv_M2_mt2.out.txt`、`gemv_M4_mt4.out.txt`、`ncu_gemv_r2.out.txt`）。**注意：下一篇候选是文章 45（主题 25d MLA decode WS）或文章 46（主题 26e GEMV 补 1.5× gap：W4A8+dp4a）。**

- 2026-09-22：完成并发布 **文章 43（主题 26c）W4A16 的跨 item 持久化流水：把 wave 2.58 压到 1，再跟每 stage 的除法/原子税算账**：shape 取自 `/ssd/models/qwen3-8B/config.json`（`hidden=5120, intermediate=17408, group=128`，对称 int4），`M=1/4/16/32/64, N=17408, K=5120`。①**设计**：把 `(item, kb)` 展平成一维 stage 流，一个 CTA 静态 round-robin 领多个 `(n_tile, k-chunk)`，producer/consumer 只认全局 stage 下标 `t`、跨 item **不停机**；item 边界只在 consumer 做一次 epilogue；累加器用 `wgmma` 自身的 `scale_d=0` 清零（避 ptxas `C7514`）。②**最大坑**：epilogue 若写在 flat 热循环内，寄存器 **90→207**、occ 2→1 CTA/SM、慢 40%（删掉 epilogue 冒烟编译即露出 80 regs）；改成 item 外层/kb 内层 + `#pragma unroll 1` 后回到 90 regs/0 spill。③**结果**：`M=1`（真 decode）wave **2.58→1.00**、occ 21.6%→24.1%，**0.0788 ms / 565.5 GB/s**，为 41 篇 M=1（0.0823/541.8）的 **1.04×**；`M=4` 1.028×、`M=16` 1.019×；**交叉点在 M=16~32**——`M≥32` 归约税 ∝`M×N` 反超（M=64 baseline 0.0947/470.4 vs persist c16 0.0980/454.7）。④**控制实验（关键）**：同样细的 chunk，baseline 把 `KSPLIT` 加到 8/16 反而更慢（0.0832/0.0883），证明收益来自「不重启流水」而非「切得细」。⑤**3 CTA/SM 被寄存器墙否决**：`STAGES=2` 想挤 3 CTA/SM → `acc[64]`+描述符需 ≥90 regs，门槛 `65536/(3×256)=85`，ptxas `C7602 Insufficient registers`。⑥**ncu 与 event 反号**：ncu 单次 base 80.7µs < persist 83.2µs（持久化多出的每-stage 整数除法被锁频放大，Compute 47%→51%），event 50 连发稳态则 persist 快 2.6%——decode 取 event 稳态口径并记录分歧。⑦`CHUNK < STAGES` 会 illegal memory access（相位错乱），已 `static_assert`。代码 `code/kernel-opt/43-w4a16-persist/`（`w4a16_persist.cu`、`ws_M1/M64.out.txt`、`msweep.out.txt`、`ncu_*_M1*.out.txt`、`3cta_probe.out.txt`）。**注意：下一篇候选是文章 44（MLA decode WS）或文章 45（W4A16 按 M 自适应）。**

- 2026-09-22：完成并发布 **文章 42（主题 25c）MLA decode 的 wgmma 改造：消掉 QK dup，却撞上 V 转置税，再用混合 mma 绕开**：真实 shape 取自 `/ssd/models/DeepSeek-V4-Pro/config.json`（`H=128,DC=512,DR=64,DV=512,Hkv=1`），`B=64/128, L=4k–32k`。①**纯 wgmma SS**（`wgmma.m64n{k}k16`，拆键消 prefill 融合的 QK dup）**反而更慢**：Hopper `wgmma` 的 B 只认 K-major（J4），PV 必须把 `c_kv[KT,DC]` 转置成 `V^T[DV,KT]`——每 tile 4096 次「8 条跨 `DC·2=1024B` 的标量 global 读 + 一次 SW128 16B 写」→ 拆键 4WG **181.3** / 2WG 165.7 / dup 159.5（全不如 39 的 189.9）；ncu `long_scoreboard` **2.20**、tensor 19.1%。②**诊断**：保留骨架、短路每轮 V 重载 → 天花板 **281.5（4WG）/ 368.2（2WG）TFLOPS**，一步锁定转置是全部瓶颈。③**混合解法**：QK 保持 wgmma（A=Q、B=K 均 SW128，B 天然 K-major），PV 用 `mma.m16n8k16 + ldmatrix.x4.trans` **直接从同一块 SW128 K tile 读 V**（SW128 只是 16B 粒度 `c'=c^r` 置换、每个 16B chunk 完好，用 `sw128_off(key,dv,DK)` 给地址即可）→ V 一 tile 只从 global 读一次、零转置；④再叠 **K 双缓冲**（混合版 smem 只剩 `Q73.7K+K73.7K+P9K`，第二块 K 恰好塞下：`3×73728+9216+red = 232448B` 顶格）→ `long_scoreboard` 2.20→**0.10**、tensor 19.1%→**36.7%**；⑤结果 **2.009 ms / 290.7 TFLOPS（29.4%）/ 1202 GB/s** @B64L32k，为 39 的 **1.53×**（L4k 1.49×、B128 1.54×、B16 1.50×）；ncu DRAM 34.3%、occ 12.5%、254 regs、stall `wait` 0.83 / `not_selected` 0.45 成下一道墙；⑥**FP8 仍不划算**（KV 仅 35.9% HBM、tensor 36.7%，两项都不到半饱和）——延续 39 的判据；⑦坑：`mma16816` 的累加器指针必须 `O+(dn*2)*4`（扁平数组写成 `O+dn*2` 会让相邻 n8 tile 重叠）、跨 WG 的 `red` 行号 `r0` 已是 CTA 内行号别再加 `16*W`（长序列误差 7e-5 掩盖、L=64 单 tile 相对误差 13% FAIL）、`MaxDynamicSharedMemorySize` 上限恰是 232448B。代码 `code/kernel-opt/42-mla-decode-wgmma/`（`mla_decode_hybrid.cu`、`mla_decode_wgmma.cu`、`pvt_test.cu`、`probe/probe.cu`、`hybrid_*.out.txt`、`ncu_*.out.txt`）。

- 2026-09-21：搭建容器 `kernel_lab` 与 `scripts/`、`common/cuda_utils.cuh`，起草路线图。
- 2026-09-21：完成并发布 **01–08**（… / Softmax / GEMM 入门）。已 push 且线上 200。
- 2026-09-21：完成并发布 **09 GEMM 进阶**：寄存器分块 8×8 + 交错映射消 bank conflict → 39.2%；float4 向量化 → 45.0%；cp.async 双缓冲 → **51.5%**（34.46 TFLOPS）；同口径 cuBLAS 75.8%（50.73 TFLOPS），达其 ~68%。
- 2026-09-21：完成并发布 **10 ncu 深潜**：手算 occupancy、`__launch_bounds__` 反优化（spill 1.31 TB 慢 34×）、五类 stall 指纹、`Memory Throughput` 语义澄清。
- 2026-09-21：完成并发布 **12 异步拷贝与流水线**：模板化 N 级软流水（`gemm_pipe<STAGES>`）：sync 30.18% → pipe4 **53.6%**（35.84 TFLOPS）；`long_scoreboard` 0.91→0.03；达同口径 cuBLAS 70.6%。
- 2026-09-21：完成并发布 **13 Tensor Core 入门**：WMMA(62)→mma+ldmatrix(115)→+cp.async **220.98 TFLOPS / 22.3%**；关键杠杆是 smem 行距 +8 padding 消 ldmatrix bank conflict（3565 万→0，2.9×）；同口径 cuBLAS BF16 672.70（68%）。
- 2026-09-21：完成并发布 **11 launch 配置与 occupancy**：ILP×occupancy 可互相替代；`__launch_bounds__` 扫描证实强制提 occupancy 负优化；thread tile 2×2(100%occ)18.8% vs 8×8(25%occ)46.8%。
- 2026-09-21：完成并发布 **14 融合与 epilogue**：GEMM+bias+GELU 仅慢 ~1%（218.61 TFLOPS）；独立 epilogue kernel ~4 TB/s；K 越小融合越值钱（11%/26%/33%）。
- 2026-09-21：**用户要求扩展**：新增第五/六/七部分（模型场景算子 + 极致性能）；新增台账 `TECHNIQUES.md`；autopilot 改为不停机、自我扩充。
- 2026-09-21：完成并发布 **15 MLA 注意力（一）**：读 `~/github/FlashMLA` 与 `/ssd/models/*/config.json` 对齐 MLA 吸收形式（V3 MH=128/DC=512/DR=64/DV=512，V4 448/64，Kimi 64head）；三个标量实现（naive/head_reuse/smem）全在 15~19 TFLOPS（ncu 证实 `Compute` 77~83%、`DRAM` 0.7%→算力受限，KV 驻 L2）；TC 三 kernel（QKᵀ+softmax+PV）**2.53ms / 115.64 TFLOPS（11.7%）**，比标量最好 `head2` 快 6.1×，距 FlashMLA 660 约 5.2×；扫 S=1024/2048/4096 暴露 S/P 物化流量 ∝S²（S=4096 占 21%），指出融合方向。
- 2026-09-21：完成并发布 **16 MLA 注意力（二）· 单 kernel 融合**：online softmax 融进 QKᵀ epilogue（S 只留寄存器）+ 累加器 `C`→PV `A` 片段零 shuffle + KV 常驻 smem。扫 7 个配置得 `f4`(dup2) 146.5、**`f4s`（smem 共享 P，QK dup=1）170.1 TFLOPS/1.717ms（S=1024），Sk=4096 → 184.7，相对 15 三 kernel 1.47×/5.78×**；ncu：DRAM 仅 7%、L1/TEX 63%、tensor 26.7%、occupancy 12.5%、`short_scoreboard` 3.05 主导。额外实现 `wgmma` 版（SS，2 warpgroup，K-major INTERLEAVE 布局）实测 **84.6 TFLOPS**——暴露无 swizzle 布局的 smem store bank conflict（2.7e8）与操作数读取低效，结论：wgmma 胜负手是 SW128 swizzle。距 FlashMLA 640 收窄到 **3.5×**。踩坑：`S` wgmma/mma 累加器每块必须清零；CUDA 13 nvcc 不认 `-arch=sm_90a`，要 `-gencode=arch=compute_90a,code=sm_90a`（`run.sh`/`ncu.sh` 已支持 `ARCH=""`）。

- 2026-09-21：完成并发布 **文章 17（主题 24）Muon / MuonClip 的 Newton–Schulz 正交化**：还原成 5 步 × 3 GEMM = `30N³` 的链式算子（Kimi-K2.6 hidden=7168/18432、moe=2048、384 experts；DeepSeek-V4-Pro 7168）。三条路径对拍（自研 fused / cuBLAS+elementwise / fp32 参考）：N=4096 自研融合 **244 TFLOPS（24.7%）/ 8.44 ms**，融合 epilogue（`f·x+g`）比不融合快 5.5%；单 GEMM 自研 269 vs cuBLAS 883（峰值 89.3%）= 30.5%；端到端 cuBLAS 链 529 TFLOPS（2.17×）。ncu：L2 76%、occ 23.8%、Compute 45%、DRAM 13% → `mma` 路径天花板，出路 wgmma+TMA。踩坑：多级 `cp.async` 的 `wait_prior` 应为 `STAGES-1`（写成 `-2` 读到半写数据）；手搓多级 smem 时 B 的 stage 基址要乘 `STAGES`。代码 `17-muonclip-ns/`（`ns_all/sweep/single/ncu.out.txt`）。**注意：主题号与文章号已解耦，下一篇是文章 18。**
- 2026-09-21：完成并发布 **文章 18（主题 17 上半）DSA 稀疏注意力（一）lightning indexer + exact top-k**：真实 shape 取自 `/ssd/models/DeepSeek-V4-Pro/config.json`（index_n_heads=64, index_head_dim=128, index_topk=1024）。①indexer 是「H^I 个小 GEMM 共享同一个 K」：标量 0.93 → TC 222 → **HG=2+BN=128 287–311 TFLOPS（31% 峰值）**；两条杠杆 head 合并（消 K 重复 ldmatrix，ncu L1 85%→62%）、加宽 BN；反例 HG=4/BN=128 溢出→69；网格顺序 `grid.x=KV` 让 Q 常驻 L2（32k 上 69.7→214，3.1×）。②exact top-k 用 radix-select（保序 uint32 + 4 趟 8-bit 直方图 + 收集），独占瓶颈是 shared atomicAdd：单直方图 26.7ms → **per-warp 私有直方图 6.75ms（3.96×，等效 3.2TB/s/95% HBM）**；整行塞 smem 长序列反慢（8.23 vs 6.75，occ 锁死 1 block/SM）。③DSA 预算（indexer+topk+稀疏 MLA 估算）相对稠密 MLA `f4s`：4k 2.9× / 16k 9.7× / 32k 14.8× / **64k 18.6×**，上限 ~30%（被 indexer O(S²) 封顶）。代码 `18-dsa-sparse/`（`dsa_S*.out.txt`、`indexer_ncu/topk_ncu/dense_mla_f4s.out.txt`）。踩坑：保序变换的逆不自逆（mask 依赖符号位，写错 out_val 全错）。
- 2026-09-21：完成并发布 **文章 19（主题 17 下半）DSA 稀疏注意力（二）稀疏 MLA 消费端**：真实 shape 同 V4-Pro（H=128, DC=512, DR=64, DV=512, topk=1024）。CTA = 「1 个 query token × $B_H$ 个 head」（同 token 全 head 共享 $I_t$，gather 一次喂满 block）；在 16 篇 `mla_shared_kernel` 上加①逐 key gather（行内 `uint4` 合并、行间随机、`topk_idx` 同址广播）②尾部 tile 掩码（已知 `valid`，前 ⌊valid/KT⌋ 块全有效、跳过掩码，113→133）③`cp.async` 双缓冲（`long_scoreboard` 4.56→1.46，64k 反超同步 11%）。扫配置：`KT` 比 `DVGRP` 重要，`s3`(BH=32) 因 4× 重复 gather 掉到 66；最佳 **p2 = 136.3@4k / 127.9@64k TFLOPS（13.8% 峰值）**，为稠密 `f4s` 的 ~75% 效率、工作量 1/(Sk/k)。sparse attention 相对稠密实测：4k 3.0× / 8k 5.9× / 16k 12.9× / 32k 25.2× / **64k 48.6×**；DSA 端到端（+18 的 indexer/topk 折算）4k 2.5× / 32k 13.3× / **64k 17.3×**，逼近 18 的 ~31× 上限。距 FlashMLA sm90 sparse prefill 640（H800）约 **4.8×**。代码 `19-dsa-sparse-attn/`（`sparse_S32768.out.txt`、`sweep_sk.out.txt`、`sparse_S32768_K512.out.txt`、`dense_mla_f4s_1024.out.txt`、`ncu_sk8192/65536.out.txt`）。
- 2026-09-21：完成并发布 **文章 21（主题 32/33/39 上半）MLA 极限冲刺（二）：修 V 转置访存 + `cp.async` 单缓冲预取 K**：真实 shape 同 20（H=128, DC=512, DR=64, DV=512）。①**诊断**：20 篇的 V 转置 `for(i){dv=i/(KT/8);kb=i%(KT/8);...ckv[(k0+kb*8+j)*DC+dv]}` 相邻线程地址差 `8*DC*2`=8KB，完全碎片化；ncu（Sk=4096）实测 `l1tex__throughput` **73.4%**、tensor pipe 18.2%、`long_scoreboard` **4.81**（等 global）。②**修法①**：把 `i` 高低位互换（`kb=i/DV; dv=i%DV`）→ 相邻线程读相邻 `dv`（差 2B、warp 覆盖 64B），写侧 SW128 散布仍 4-way（理论下限）；指令/寄存器不变，**105.3→142.3 TFLOPS（+35%）**；ncu：L1 45.8%、tensor 24.4%、long_scoreboard 2.50。③**修法②**：论证「K 只被 QK 读、`wgmma_wait0` 后空闲、PV 不碰 K」→ 用 `cp.async.cg.shared.global` 16B 在 softmax/P/PV 期间把下一块 K 覆盖写进同一块 `ks`（**单缓冲即可**），`cp.async.wait_group 0` + `__syncthreads` 保证可见；V 仍需 PV 后才能覆盖。→ **159.8/193.2/198.8 TFLOPS（Sk=1k/4k/8k）**，ncu：long_scoreboard **1.42**、tensor **30.0%**、L2 33.6%、L1 46.3%、barrier 1.17/wait 1.01 成下一道墙；**Sk≥4096 反超 16 篇 `f4s`**（183.8/173.9），距 FlashMLA ~640 收窄到 **~3.3×**（20 篇为 6.1×）。④**岔路记录**：尝试 MN-major（免转置）GMMA 描述符（`mn128.cuh`/`mn_test2.cu`）——cute `make_gmma_desc<Major::MN>` 的 LBO/SBO 与硬件解释相反，cute 值直接 out-of-range、强行对调后结果错乱，**未跑通**（见台账 J3），故保留转置+合并。代码 `21-mla-wgmma-pipe/`（`mla_pipe.cu`、`mla_pipe3.cu`、`sweep.out.txt`、`ncu_20baseline/ncu_pipe/ncu_pipe3.out.txt`）。
- 2026-09-21：完成并发布 **文章 20（主题 16b/31 上半）MLA 极限冲刺（一）wgmma + SW128 swizzle**：把 `wgmma` 操作数布局从无 swizzle 的 K-major `INTERLEAVE` 换成 **K-major SW128**（`layout_type=1`，物理 16B 列 `c'=c^r`），依据 CUTLASS canonical 布局推导描述符（`LBO=1`、`SBO=(K/64)*1024`、k16 步进 `floor(s/4)*1024+(s%4)*32`）。①冒烟 GEMM `gemm_sw128.cu` 一次验证对：**148.66 @2048³ / 132.04 TFLOPS @4096³**；②`qk576_test.cu`（9 atom、36 k16 步）err 1.6e-7、`pv_test.cu` err 1.5e-8 分别锁死 QK/PV；③移植融合 MLA `mla_wgmma_sw.cu`：**105.30 / 112.91 / 119.89 TFLOPS（Sk=1k/2k/4k）**，比 16 篇 wgmma INTERLEAVE 84.6 **+24.5%**，逐位对齐 `f4s` 误差。④踩坑（已入台账 G12）：nvcc 把相邻两个 bf16 标量存储合并成 `st.shared.u32` 并**丢高 16 位**，P 写回 3553 字节错位；改 `__floats2bfloat162_rn` 打包 u32 → 0 错位（`bf16_store_pitfall.cu`）。⑤ncu：store bank conflict 2.7e8→4.4-way（理论下限）、L1/TEX 86%→70.8%，但 DRAM 5.1%、Compute 18.5%、occ 12.5%、205 reg、221KB smem（1 CTA/SM）、No Eligible 80.9%、47.8% 停 L1TEX（global load 未流水）→ 仍 1.62× 慢于 `f4s` 170、距 FlashMLA ~640 约 **6.1×**。结论：SW128 是必要的但不是充分的——瓶颈转为「1 CTA/SM + 单缓冲」；下一步 TMA/多级流水/warp specialization。代码 `20-mla-wgmma-sw128/`（`gemm_sw128.out.txt`、`small_tests.out.txt`、`mla_wgmma_sw.out.txt`、`mla_sw_ncu.out.txt`、`bf16_store_pitfall.out.txt`）。

- 2026-09-21：完成并发布 **文章 22（主题 22）FP8 GEMM（一）：e4m3 的 mma.sync 与 wgmma + per-tensor / per-block 缩放**：真实 shape 取自 `/ssd/models/DeepSeek-V4-Pro/config.json`（hidden=7168, moe_inter=3072, 384 experts top-6；e4m3 + ue8m0 + weight_block 128×128 + dynamic 1×128 激活），测 `M=4096,N=3072,K=7168`（180.4 GFLOP，AI≈1785 → 算力受限）。①`mma.m16n8k32` 版：FP8 片段用「两个 fp8 = 一个 b16」复用了 `ldmatrix`（`x4` 取 a0..a3、`x2` 取 b0/b1），smem 行距 BK+16 消 bank；`cp.async` 4 级流水 → **266.3 TFLOPS（13.5%）**；ncu：Compute 57%、tensor pipe 41%、`math_pipe_throttle` 1.12 + `wait` 1.59、DRAM 仅 7.9% → **发射端口受限，mma.sync 在 Hopper 打不满 FP8**。②换 `wgmma.m64n128k32`：**FP8 的 K-major SW128 atom 与 bf16 逐字节同构**（8 行×128B，只是每行 128 个 e4m3），20 篇描述符原样复用；主循环用 `wgmma.commit_group` + `wait_group(STAGES-2)` 代替每块 `wait0`（acc 到最后才读）→ **602/717/768 TFLOPS（s2/128×128s3/256×128s3）**，相对 mma.sync **+2.89×**，**达 cuBLAS FP8（1381.7）的 55.6%、同 shape cuBLAS bf16（800.7）的 96%**。③per-block（1×128 激活 + 128×128 权重，`sb` 每 n-tile 退化成标量）→ **519 TFLOPS（26.2%）**，每块折算 `wait0` 切断流水。踩坑：①相邻两列 c0/c1 各有 `sb`，共用一个 scale 只在逐列随机 scale 时暴露（常数 scale 会「假通过」）；②FP8 wgmma asm 尾部是 `p,scaleA,scaleB`（3 个，非 bf16 的 5 个）；③`__launch_bounds__` 写死 256 而 BM=256 需 512 线程时只算一半行、性能虚高且采样巧合通过（修正后 256×256 真值 218）。ncu（wgmma 版）：Compute 38.5%、occupancy 24.7%、No Eligible 59.9%、1 CTA/SM（regs+smem 双限）。代码 `22-fp8-gemm/`（`fp8_gemm.cu`、`fp8_gemm_wgmma.cu`、`cublas_fp8_ref.py`、`fp8_mma/fp8_wgmma/cublas_fp8/ncu_mma/ncu_wgmma.out.txt`）。

- 2026-09-21：完成并发布 **文章 23（主题 23/32/33）FP8 GEMM（二）：TMA + mbarrier + warp specialization**：真实 shape 同 22（取自 `/ssd/models/DeepSeek-V4-Pro/config.json`：hidden=7168, moe_inter=3072，e4m3 + ue8m0 + weight_block 128×128）测 `M=4096,N=3072,K=7168`。①A/B 装载从 22 的「256 线程各自算 SW128 地址 + `LDGSTS.16B`」换成 **TMA `cp.async.bulk.tensor.2d` + `CU_TENSOR_MAP_SWIZZLE_128B`**——TMA 硬件写出的 smem 布局与 wgmma 的 K-major SW128 描述符逐字节一致，kernel 里零 swizzle 代码（`make_desc_sw128` 原样复用；驱动枚举无 FLOAT8，用 `UINT8` 搬字节，需 `-lcuda`；tensormap 作 `__grid_constant__` 参数）。②**warp specialization**：1 producer warp 专职 TMA（`mbarrier.arrive.expect_tx` + `full[st]`），`(BM/64)` 个 consumer warpgroup 只做 wgmma，用 `empty[st]`（count=消费者线程数）回压，消费者以 `wgmma.wait_group<STAGES-2>` 决定释放哪个 stage。③**两个关键坑**：相位数组 `phase[st]` 动态下标掉 **local memory**（ncu 报 local memory 占 L1TEX 47.5% sector、`long_scoreboard` 8.1，只有 988）→ 改成常量相位 `(kb/STAGES)&1`；epilogue 标量 store 半 sector（ncu 报 50% excessive）→ `float2` 向量存储。④结果 per-tensor **1217.3 TFLOPS（61.5%）**（22 的 768.3 → +58%），达 **cuBLAS FP8（1381.3）的 88.1%（差距 1.13×）**、超过同 shape cuBLAS bf16（800.4）的 1.52×；扫配置最佳 `256×128×BK128 s4`（smem 197KB、1 CTA/SM），对比 `128×128 s3`（2 CTA/SM 但 L2 80%）证明「降 L2 流量 > 堆 occupancy」。⑤per-block（1×128 激活 + 128×128 权重）**519 → 906 TFLOPS（+74%）**，ncu 寄存器 150、occ 13.8%，瓶颈转为每块折算等 mma。ncu（最佳）：Compute 67.8%、tensor pipe `hmma_cycles_active` **72.0%**、DRAM 27%、L2 56.7%、local mem 0、`long_scoreboard` 9.15。代码 `23-fp8-gemm-tma/fp8_gemm_tma.cu`（`fp8_tma.out.txt`、`ncu_tma256x128s4/ncu_tma128x128s3/ncu_pb128x128s3/ncu_stalls_best/cublas_ref.out.txt`）。
- 2026-09-21：完成并发布 **文章 24（主题 23b）FP8 GEMM（三）：per-block 缩放的寄存器墙 + ptxas 序列化**：真实 shape 同 22/23（`M=4096,N=3072,K=7168`，取自 `/ssd/models/DeepSeek-V4-Pro/config.json`）。①**同二进制对照**：同一 kernel 加 `MODE=3`（读 `sa/sb` 但不折算、`wait_group<STAGES-2>` 流水）作 per-tensor 基准，锁死差距——128×128 s3：per-tensor **1090.8** vs per-block **913.3**（−16.3%）；per-tensor 256×128 s4 **1209.6**。②**根因=寄存器→occupancy**：per-block 多一个跨 k 保留的 fp32 `fin`（每线程 64 regs），288 线程寄存器 **90→154**，2 CTA/SM（113 门槛）→ **1 CTA/SM**；ncu：Compute **67.7%→49.7%**、L2 57%→65%、occ 25.4%→13.7%。③**BM=256 对 per-block 不可行**：`acc[64]+fin[64]=128` regs × 544 线程 > 120 预算，实测 96 regs + 608B spill → **224 TFLOPS**；折中 BM=192 → 683–699。④**ping-pong/pair-drain 重叠折算失败**：读飞行中的 wgmma 累加器触发 ptxas `C7514`/`C7511` 把 wgmma 串行化，358/397（比老实 `wait0` 的 913 慢 2.3×），寄存器 168+spill。⑤**负结果**：scale 预取 smem 913→871（更慢）、`__launch_bounds__(288,2)` 强压 2 CTA → 219.7（spill 608B）。⑥**per-block 最佳 936.5（128×128 s4，47.3%）**，+3.3% vs 23 篇 906；同配置 per-tensor 938.7（折算在该 config 几乎免费）。对标：cuBLAS FP8 per-tensor 1379.5（本篇 67.9%）、per-row 1335.9（便宜算子，仅上界参考）。代码 `24-fp8-gemm-pb/fp8_gemm_pb.cu`（`fp8_pb.out.txt`、`ncu_pb_s4.out.txt`、`ncu_pt_s4.out.txt`）。

- 2026-09-21：完成并发布 **文章 25（主题 21）MoE（二）：grouped GEMM**：真实 shape 取自 `/ssd/models/DeepSeek-V4-Pro/config.json`（hidden=7168, moe_inter=3072, 384 routed, top-6）。①**基线**：per-expert loop（384 次 launch，每 expert 仅 ~24 CTA）耗时对 token 数几乎不敏感（8k/16k/32k = 13.18/13.19/14.08ms）；②**分组 kernel**：把 expert 编码进 B 的行坐标 `b_row=group*N+blockIdx.x*BN`（B 在显存里是 (G,N,K)≡(G*N,K)），A/B 都只用 2D TMA，`group` 由 `grouped_layout[m_tile*BM]`（contiguous）或 `blockIdx.z`（masked）给出，复用 23 篇 TMA+mbarrier+warp specialization；③语法/语义坑：**TMA `boxR` 必须随 BN 建**，否则 `expect_tx` 补不齐 → **不报错只是死锁**（BN=256 全挂）；3D TMA 能表达分组但 2D 行折叠更省事、同性能；④**结果**：prefill 相对 loop **3.59×/2.36×/1.47×**，最佳 958–1008 TFLOPS（aligned，48–51%），cuBLAS per-expert graph loop 6.54ms（822.8）→ 归一化后我们 **1.17×**；⑤**masked decode** 纯权重带宽，读 8.45GB 达 **2.8TB/s（83.6% HBM）**、含 D 写 ~89%，比 cuBLAS padded batched（graph 4.62ms）快 **1.53×**；⑥contiguous 对齐 padding：32.5%/19.8%/10.8%（8k/16k/32k）；⑦ncu（最佳）DRAM 52%、**L2 81.9%**、tensor 61.3%、occ 13.9%（1 CTA/SM）、`long_scoreboard` 47.8% → B 被每个 m-tile 重读、L2 流量放大 ~4.5×，下一步 **TMA cluster multicast**。代码 `25-moe-grouped-gemm/`（`moe_S{8192,16384,32768}.out.txt`、`cublas_moe.out.txt`、`cublas_moe_ref.py`、`ncu_grp128x256s3.out.txt`）。
- 2026-09-21：完成并发布 **文章 26（主题 21b）MoE grouped GEMM + TMA cluster multicast：把 L2 墙试一试**：真实 shape 同 25（`/ssd/models/DeepSeek-V4-Pro/config.json`：G=384 top-6, K=7168, N=3072）。①**沿 N 的 A 广播**（`cluster.x=CN`）：A 只由 m-tile 决定，沿 N 的 cluster 一定共享 A、不受 expert 分组边界影响；用 `cudaLaunchKernelEx`+`clusterDim`、leader 发 `cp.async.bulk.tensor.2d...multicast::cluster`、每 CTA 各自 `arrive.expect_tx`、共享 operand 的释放用 `mapa.shared::cluster` arrive 到 leader、私有/共享 empty barrier 分离、init 后与退出前各一次 `cluster_sync`。②**协议跑通且与 CN=1 逐位一致**；ncu：CN=2 把 A 的 L2 读 sector 砍 **15–19%**、`long_scoreboard` 11.6→8.0。③**但只有大 batch 兑现**：32768 tokens+128×128 从 960.7→**1001.6 TFLOPS（+4.3%）**；8192 867.8→804.0（−7.4%）、16384 947.7→926.8（−2.2%）、masked decode 174.2→167.5（−3.9%，纯 DRAM 带宽场景无用）、CN=4 全线更慢。④**根因**：`lts__throughput`（请求/延迟）与 `lts__t_sectors`（字节）不同——省字节但 L2 占用不降（16k 94.08%→94.36%），跨 CTA 耦合还让 tensor 62.5%→59.7%。⑤**BM=256 负结果**：padding 10.8%→19.6%，256×256 触发 ptxas `C7511` wgmma 串行化崩到 124。⑥**对标**：16384 grouped 领先 cuBLAS graph 1.16×；32768 cuBLAS graph 追平（1028.8 vs 1001.6）。代码 `26-moe-cluster-multicast/`（`moe_cluster.cu`、`moe_cluster_launch.h`、`moe_cluster_S{8192,16384,32768}.out.txt`、`ncu_c1_c2_16384/32768.out.txt`）。**注意：下一篇是文章 27。**

- 2026-09-21：完成并发布 **文章 27（主题 20）MoE（一）：router + top-k + token 置换**：真实 shape 取自 `/ssd/models/DeepSeek-V4-Pro/config.json`（hidden=7168, n_routed=384, topk=6, `scoring_func=sqrtsoftplus`, `topk_method=noaux_tc`, `norm_topk_prob=true`, `routed_scaling_factor=2.5`）。手写七个 kernel 还原 noaux_tc 前门：①**gate GEMM**（`mma+ldmatrix+cp.async` 双缓冲，BM=128/BN=128；helper 模板化）**204→257 TFLOPS**（M=16k/32k，20.7–25.9% 峰值），仅 cuBLAS 738.2 的 **27.7%**；ncu：L2 60.7%、Compute 35.3%、No Eligible 57.6%、occ 20%（2 CTA/SM）；BM=256（512 线程/127 寄存器）掉到 1 CTA/SM 反而 **−10%**（负结果）。②**router top-k**（`sqrtsoftplus+bias` 选专家、权重用未加 bias 的 `s` 归一化 ×2.5）：1 warp/token、8 warp/block，0.031 ms@16k，CPU 对拍 top-6 集合 8/8、权重 err 6e-8；ncu Compute 62.6%、occ 84.7%。③**路由元数据**（直方图+前缀和+atomic 游标 scatter）融进 top-k lane0，整条 route 0.0879→0.0710 ms@16k（−19%）、0.175→0.127@32k（−27%）。④**permute**：「1 block/token 读一次写 K 行」比「1 block/行（读 x K 次）」**1.53×**（0.924→0.605 ms@16k，1.64 GB vs 2.82 GB，81% HBM；`stcs` 只快 0.6%）。⑤**unpermute** 融合加权 **0.535 ms / 3075 GB/s / 91.7% HBM**（ncu DRAM 91.9%）。⑥端到端 M=16384：gate 0.439 + route 0.071 + permute 0.605 + unpermute 0.535 = **1.651 ms**，**搬运占 69%**；对标 torch（cuBLAS+topk+index_select+index_add）：gate 0.28×、permute 1.50×、unpermute **17.8×**、端到端 **6.6×**。代码 `27-moe-router/`（`moe_M{4096,8192,16384,32768}.out.txt`、`check_M16384.out.txt`、`ncu_{gate,perm,unperm,topk}.out.txt`、`torch_ref_M16384.out.txt`）。**注意：下一篇是文章 28。**
- 2026-09-21：完成并发布 **文章 28（主题 20b）MoE（一·续）：wgmma + TMA 版 gate GEMM**：真实 shape 同 27（`X[M,7168]·Wg[384,7168]ᵀ`，M=16k/32k）。①**`wgmma.m64n128k16` SS + SW128**（bf16 atom 8 行×64 元素，故 `BK=64`；**权重 `Wg[E,H]` 天然 K-major，B 免转置**）→ 337.1/359.6 TFLOPS，比 27 的 `mma.sync`（201.9/262.3）**+67%/+37%**；`s3`（2 CTA/SM）胜过 `s4`（1 CTA/SM）。②**TMA（`cp.async.bulk.tensor.2d` + SW128）+ mbarrier + warp specialization**（1 producer warp + 2/4 consumer warpgroup，相位用 `(q/STAGES)&1`）→ **565.4@16k（128×128 s3，57.2% 峰值）/ 600.3@32k（256×128 s4，60.7%）**，寄存器 90/0 spill；**达 cuBLAS bf16（734.6/752.6）的 77.0%/79.8%**，相对 27 **2.80×/2.29×**。③**ncu（32k 最佳）**：Compute 69.6%、tensor pipe **68.2%**、L2 **79.1%**、DRAM 53.4%、occ 26.1%、0 bank conflict、`long_scoreboard` 主导 → **剩余 gap 是 A 被 3 个 n-tile 重读（L2 79%）**。④**负结果**：split-K（k2 429.8、k4 347.8，加剧 L2 竞争）、`BM=64` 420–450、`L2_PROMOTION_L2_256B` 576.3、`BN=384`（TMA box 单维上限 256）。代码 `28-moe-gate-wgmma/`（`gate_wgmma.cu`、`cublas_gate_ref.py`、`gate_M{16384,32768}.out.txt`、`ncu_ws128s3_M16384/ws256s4_M32768.out.txt`、`cublas_gate.out.txt`）。**注意：下一篇是文章 29。**

- 2026-09-21：完成并发布 **文章 29（主题 20b-2）MoE gate GEMM + TMA cluster multicast：广播 A 还是广播 B？**：真实 shape 同 28（`X[M,7168]·Wg[384,7168]ᵀ`，M=16k/32k）。在 28 的 `gate_ws_kernel` 上加 `AXIS` 选轴（`gate_mcast_kernel<BM,BN,BK,STAGES,CN,AXIS>`，AXIS=0 无 / 1=`cluster.x` 广播 A / 2=`cluster.y` 广播 B），协议同 26。①**A-multicast（CN=3）**：L2 读 sector 46.2→**34.0 M（−26%）**、L2 吞吐 80.4%→48.5%，但 duration 290→371 µs（**−23.4%**），tensor/SM 68.8%→51.5% —— 28 篇猜的「A 是墙」被证伪，瓶颈是 tensor 不是 L2 字节。②**B-multicast（CN=2）**：128×128 s3 从 556.3→**587.2（+5.6%）**，L2 读 sector 71.2→64.1 M（−10%）；但 **256×128 s4 无 mcast 就 612.2**，更高——「放大 BM」与「广播 B」是同一优化（都在减少 B 的 `M/BM` 次重读），前者无耦合。③**wide-N（一个 CTA 算完 N=384）失败**：3 个 n128 累加器 = 192 regs → ptxas `C7511` 把 wgmma 串行化（128/256 崩到 145/41）。④ncu（最佳 256×128 s4）：Compute 69.5%、L2 80.4%、DRAM 54%、occ 26.1%、`long_scoreboard` **10.85**（等 wgmma 操作数）、STAGES 已到 smem 上限 → Hopper 单 CTA/单缓冲几何的 wgmma 天花板。⑤产出「**该不该上 cluster multicast**」判据表：只在瓶颈= L2 字节、且 BM 无法再放大、且从小 cluster（CN=2）起试时才值得。**本系列 gate 最好 612.2 TFLOPS@32k（cuBLAS 81.3%）/ 547.0@16k（74.5%）。** 代码 `29-moe-gate-multicast/`（`gate_mcast.cu`、`gate_M{16384,32768}.out.txt`、`ncu_best_c1.out.txt`、`ncu_multicast_cmp.out.txt`）。**注意：下一篇是文章 30。**

- 2026-09-21：完成并发布 **文章 30（主题 20c MoE（三））fused expert FFN：gather 输入 + SwiGLU epilogue + unpermute 融合**：shape 同 25/27（`/ssd/models/DeepSeek-V4-Pro/config.json`：hidden=7168, inter=3072, 384 experts top-6）。把 27 篇的五段流水（permute→up/gate→swiglu→down→unpermute）融成 3 kernel，两版**共用同一份 GEMM 代码**（模板 `FUSE`），差异只有被省掉的 `PX/G,U/D` 三块中间张量与三个辅助 kernel。①**K1**：A 按 `row_tok` **gather**（不写 `permuted_x`）+ 一个 CTA 吃下 gate/up 双累加器（N=256）直接做 **SwiGLU** 写 bf16 activation；②**K2**：epilogue 用 `red.global.add.f32`（fire-and-forget，不用 `atomicAdd` 的返回值）把 `w·acc` 归约回 token，padding 行 `w=0` 跳过；③**BN 扫参**：K2 bn256（A2 重读减半）7.57 ms / **898 TFLOPS nofuse（90.8% 峰值）/ 858 fuse**，**超同 shape 稠密 cuBLAS（711）1.26×**；ncu K2 DRAM 79.5%/L2 88.1%。④**端到端**：M=8192 balanced **22.65→22.18 ms（1.02×）**、M=16384 **45.44→42.14（1.08×）**、M=8192 random 32% padding **32.80→30.54（1.07×）**；activation 流量 **−81%**、含权重总流量 **−13%**。⑤**根因/教训**：K1 ncu **DRAM 84.5%**，要流 33.8 GB 专家权重（roofline 下限 10.1 ms，实测 13.2）——**prefill FFN 是权重带宽受限**（50.7 GB 权重 >> 1.8 GB activation），所以 27 篇「前门搬运 69%」不能外推；融合收益被锁在 activation 占比（~13%）上，甜点是 padding 跳过 + M 大时辅助 kernel 的隐藏。⑥正确性：K1 err 0.09%、K2 4e-6、fused-vs-unfused 1e-7。**踩坑**：读 bf16 到 `float` 只 memcpy 2 字节 → 高 16 位垃圾（err 8.1 假 FAIL）；相邻两列 bf16 仍需 `__floats2bfloat162_rn` 打包（20 篇坑复现）。代码 `30-fused-moe/`（`moe_fused.cu`、`cublas_ffn_ref.py`、`moe_M8192_bal/M16384_bal/M8192_rand.out.txt`、`ncu_k1/ncu_k2.out.txt`、`cublas_ffn_ref.out.txt`）。**注意：下一篇是文章 31。**

- 2026-09-21：完成并发布 **文章 31（主题 21c）MoE grouped GEMM + per-block FP8 缩放**：真实 shape 取自 `/ssd/models/DeepSeek-V4-Pro/config.json`（hidden=7168, moe_inter=3072, 384 experts top-6, e4m3 + ue8m0 + weight_block 128×128）。把 25 篇的 grouped 骨架（`b_row=group*N+n`，TMA+wgmma+WS）加上 24 篇的 per-block 折算（每 128-k 块 `wgmma_wait0` → `fin += sa*sb*acc`；`BN=128` 让 `sb` 退化成整块标量、`sa` 逐行）。①**prefill per-block 最佳 806.4 TFLOPS@32k（128×128 s3）/ 790.4@16k（s4）/ 707.7@8k（s4）**，为**同二进制 per-tensor**（980.5/960.5/777.4）的 **82.2%/82.3%/91.0%**；②根因 ncu（32k s3 同二进制对照）：寄存器 90→**155** → occupancy **27.9%→13.9%**（2→1 CTA/SM）→ tensor pipe **62.4%→46.1%**、Compute 62.2→45.6%、L2 88.5→74.8%；主导 stall 从 `long_scoreboard` **12.61→2.03** 变成 `barrier` **0.12→1.16**（每块 wait0 排空）；③**分组让 per-block 的相对损失（18%）小于稠密单 GEMM（23%）**，且 s4 时两者都 1 CTA/SM、折算几乎免费（16k pb s4 790.4 > pt s4 772.4）；④masked decode 纯权重带宽，per-block **免费**：BM=64 **2.95 ms / 2.86 TB/s / 85.4% HBM**（BM=128 只有 75.4%）；⑤负结果：`BM=64` 省寄存器但 B 重读 `M/BM` 翻倍全线更慢（8k/16k/32k 587/656/648）、`s4` 在 32k 慢于 `s3`；⑥坑：TMA 的 `boxR` 必须随 `BM` 重建（写死 128 跑 BM=64 → `expect_tx` 补不齐、**死锁且不报 error**，`device printf` 定位）；⑦对标：cuBLAS 无 block-scale 入口（`torch._scaled_mm` 只支持 TensorWise/RowWise，2D block scale 报 `Invalid scaling configuration`），DeepGEMM 因容器缺 `libdwfl` 编不出来（见「阻塞」），上界取同口径 per-tensor grouped。代码 `31-moe-grouped-pb/`（`moe_pb.cu`、`moe_pb_launch.h`、`moe_pb_S{8192,16384,32768}.out.txt`、`ncu_pb_s3.out.txt`、`ncu_pt_s3.out.txt`、`ptxas.txt`）。**注意：下一篇是文章 32。**

- 2026-09-21：完成并发布 **文章 32（主题 19）RMSNorm / QK-Norm / 融合残差 / 融合 FP8 量化：一个纯访存算子怎么贴到 87% HBM**。shape 取自 `/ssd/models/{DeepSeek-V4-Pro,Qwen3-8B,GLM-5.2-FP8}/config.json`（H=7168/5120/6144；Qwen3 40q/8kv head_dim=128）。`M=8192, H=7168`，有效口径 `2·M·H`。①**v0 朴素**（每行 block、读 x 两遍、无 smem）1481 GB/s / **44.2%**；②**v1 smem 行缓存单读** 2334 / 69.6%，但 ncu `l1tex__throughput 76%`、`sm 31.5%` → **L1 成为比 HBM 更早的墙**；③**v1r 寄存器缓存整行**（每线程 `NG=ceil((H/8)/T)` 个 uint4 留寄存器，归约后就地归一化）→ **2869 GB/s / 85.6%（ncu DRAM 82.9%，L1 降到 53.6%，48 regs）**，单步 +16pp；④**v2r 融合 `x+res`**（省「第二遍读 s」= 5 pass→4 pass）→ **2923 GB/s / 87.2%（ncu DRAM 86.2%，L2 79.7%）**；⑤**v3r 再叠 FP8 e4m3 per-128 动态量化**（`amax` 走 shared `atomicMax`、逐字节拼 `uint2`）**掉到 2205 / 65.8%（ncu DRAM 64.8%、SM 50.2%）**——只省 8B→7B 却新增 amax 原子竞争，**融合不划算**；⑥**QK-Norm**（一 warp 一 (token,head)，shfl 归约）2179 GB/s / 65.0%（ILP 低）。⑦**踩坑**：本工具链下 `float(fp8)` 返回**原始位模式**（`1.0f→56`），精度校验误差恒定 155%，必须走 `__nv_cvt_float_to_fp8` + `__nv_cvt_fp8_to_halfraw`（降到 3.31%）；`fp8 out=fp8(storage)` 构造也会走错重载。⑧**同轮附带判决 MLA 的 MN-major 免转置不可行**：CuTe 生成正确 canonical MN 描述符后定点 GEMM 实测，硬件**忽略 LBO、按 K-major 解释 B**（LBO 64/256/1024 误差逐位不变），Hopper `wgmma` 免转置 V 关闭（见 TECHNIQUES J4）。代码 `32-fused-norm/`（`norm_fused.cu`、`fp8_test.cu`、`fp8_test2.cu`、`norm_fused.out.txt`、`ncu_norm.out.txt`）。**注意：下一篇是文章 33。**
- （同轮调查，未成文）**MLA 极限冲刺（三）受阻**：①MN-major 免转置被硬件否决（同上）；②跨迭代 QK/PV 软流水（`32-mla-tma-v/mla_v3.cu`）触发 ptxas `C7515` wgmma 串行化 + 255 regs/536B spill → 147 TFLOPS（<198.8）；③向量化 V 收集→smem 散布版（`mla_v4.cu`）52.8；④`prefetch.global.L1`（`mla_v5.cu`）196.6、`__ldg` 191.9，均无收益。根因：216KB smem 锁 1 CTA/SM + V 转置不可异步。详见「阻塞」。

- 2026-09-21：完成并发布 **文章 34（主题 21d）fused MoE expert FFN 的 FP8 化**：shape 取自 `/ssd/models/DeepSeek-V4-Pro/config.json`（hidden=7168, moe_inter=3072, 384 experts top-6, e4m3+ue8m0+weight_block 128×128）。把 31 的 per-block grouped GEMM 接进 30 的 MoE FFN 端到端。①两版共用同一份 TMA+wgmma+WS 的 per-block grouped kernel，差异只有「down 是否融合 unpermute」+「swiglu+quant」；②端到端 **11.80ms @M8k bal**（bf16 30 篇 22.65 → **1.92×**）、**20.58 @M16k**（45.44 → **2.21×**）、**15.93 @M8k rand 32.4% padding**（32.80 → **2.06×**）；③专家权重 50.7→**25.4GB**；K2 down **88.1% HBM**（ncu DRAM 89.3%、L2 86%、156 regs）、K1 78%（ncu DRAM 80.1%、long_scoreboard 占 stall 51.3%、occ 13.9%）；④**负结果一**：unpermute 融合在 FP8 下反向——K2f 3.83ms vs 3.35+0.54（ncu DRAM 73.2%、**L2 94.4%**），30 篇 bf16 的 +10% 在 GEMM 变快后失效；⑤**负结果二**：per-block + 双累加器 SwiGLU = `acc(2×64)+fin(2×64)=256 > 255` 寄存器，`BN=256` 同理崩到 21.7ms；⑥附带把 swiglu+动态量化从 39%→**92% HBM**（一行一 block + `float4` + warp 内 `__shfl_xor` amax，2.4×）。代码 `34-fused-moe-fp8/`（`moe_fp8_M{8192,16384}_{bal,rand}.out.txt`、`ncu_k1/k2/k2f.out.txt`）。

- 2026-09-21：完成并发布 **文章 35（主题 17 剩余）DSA Compressor：KV 门控池化**：官方 prefill 语义取自 `/ssd/models/DeepSeek-V4-Pro/inference/model.py:279`（`class Compressor`）。①**合并投影 GEMM**（`wkv`+`wgate` 拼成 `[2C,D]`，A 只读一遍）+ TMA/wgmma/WS bf16 → **649.8/655.2 TFLOPS（65.7/66.3% 峰值，M=32768，256×128×64 s4）**，为同 shape cuBLAS 合并 bf16（809/814）的 **~80%**；两次独立 bf16 cuBLAS 0.630→合并 0.595（ratio128）/1.398→1.185（ratio4），证明 A 复用有效。②**融合池化 kernel**（online softmax + RMSNorm + RoPE，一 block 一窗、一线程一列）**2790–2812 GB/s / 83–84% HBM**。③端到端相对官方 eager **2.00×（ratio128 M32k）/2.04×（ratio4）**，M=8192 达 2.39×/2.14×；拆开：投影 1.70×、池化环节 **~6.6×**。④ncu 判决投影 **L2 受限**（`L2` 85–88%、`Compute` 57–59%、`DRAM` 26–29%、occ 28%、No Eligible 86%），根因权重 B 被 `M/BM=128` 个 m-tile 重读（L2 读 ≈7.5GB）。⑤负结果：中间张量 Y 降 bf16 打平略负（pool 读省了、GEMM 的 f32→bf16 转换又吃回去）。⑥正确性：CPU 全流程 fp64 参考对拍 err ~0.2%（含 overlap 8-slot、YaRN RoPE）。代码 `35-dsa-compressor/`（`compressor.cu`、`compressor_ref.py`、`comp_ratio{128,4}_M{8192,16384,32768}.out.txt`、`sweep_ratio{128,4}_M32768.out.txt`、`torch_ref.out.txt`、`ncu_proj_256x128s4.out.txt`、`ncu_proj_r4_256x128s4.out.txt`、`ncu_pool_r{128,4}.out.txt`）。**注意：下一篇是文章 36。**

- 2026-09-21：完成并发布 **文章 36（主题 17b）DSA compressor 投影：拆「L2 墙」三招全负 + FP8 per-block 化**：shape 同 35（`/ssd/models/DeepSeek-V4-Pro/config.json`：hidden=7168, head_dim=512, qk_rope=64, compress_ratios∈{128,4,0}；量化 `e4m3+ue8m0+weight_block 128×128+1×128 dynamic`）。①**翻案 35 的「L2 墙」**：ncu 打到最佳几何 `256×128×64 s4` 是 **tensor pipe 76.4% 受限**（L2 仅 58%），35 的 L2 84% 是 `128×128s2` 自己 `BM=128` 造成 B 重读 256×；②**三种压 L2 手段全负**——A/B TMA cluster multicast（CN=2/4/8：609/597→537/513 vs 基线 632，−3.7%~−19%）、`cudaAccessPolicyWindow` persisting（618.7，−2.2%，hit rate 已 82%）、K-split 双累加链（ptxas `C7517/C7518` 串行化，449/70）；顺带验证消费者 `fence.proxy.async` 可删（中性）；③产出「该不该上 multicast」**五条判据**（tensor>70% 就别上）；④**真正的提速=换数制**：按 V4-Pro 真实量化把投影做成 FP8 e4m3 per-block，**959.1 TFLOPS（ratio128，1.385×）/ 974.1（ratio4，1.394×）**（bf16 同进程 692.5/698.8，70% bf16 峰值；即 48.5/49.2% FP8 峰值），权重字节减半，量化后池化输出最大误差 3.2/3.8%；⑤ncu 对比：bf16 tensor 76%/L2 58%/stall `long_scoreboard` 9.56 → FP8 tensor 51%/L2 73%/occ 13.8%/stall `barrier` 1.18（per-block 寄存器墙复现）。代码 `36-dsa-compressor-mcast/`（`compressor_mcast.cu`、`compressor_fp8.cu`、`*_M32768.out.txt`、`ncu_bf16_best/ncu_fp8_best.out.txt`）。踩坑：TMA 的 `boxR` 必须随 **BM**（不只是 BN）重建，否则 mbarrier 死锁（25/31 篇坑复现，hangs 128x256/64x128/192x128）；`timeout` 杀不掉容器内死锁进程（污染后续基准，需 `docker exec kernel_lab pkill`）；长短 benchmark 差别大（200/100 warmup 会热降频 ~10%，head-to-head 必须同 warmup）。**注意：下一篇候选是文章 37（FP8 per-block 1-warpgroup/248-reg 布局）。**

- 2026-09-21：完成并发布 **文章 37（主题 21e）FP8 per-block 的第三条路：把 ue8m0 的 2 的幂 scale 折进操作数**：shape 取自 `/ssd/models/DeepSeek-V4-Pro/config.json`（hidden=7168, moe_inter=3072；`e4m3 + ue8m0 + weight_block 128×128 + 1×128 dynamic`）测 `M=4096/16384, N=3072, K=7168`。①**诊断**：fold 的 23% 差距是「折算时张量核空转」——每 128-k 块 `wgmma.wait_group 0` 后 64 条 FMA/线程，roofline 占 19%；ncu tensor pipe **49.8% vs 67.9%**、stall `barrier` **1.09 vs 0.18**。②**两条直路撞墙**：用 wgmma 自身的 `scale_d=0`（DeepGEMM 把循环下标 `k` 当 `scale_d`）替代通用 FMA 清零，**消掉了 ptxas `C7514`**；但 `setmaxnreg` 被 ptxas 以 **`C7507`**（维持最低寄存器需求）忽略，ping-pong **348** vs 基线 940。③**第三条路**：`ue8m0` 是 2 的幂 → `q·sa` 只是 e4m3 指数平移、**精确** → per-block GEMM 退化成 per-tensor GEMM，GEMM 本体 **940.6 → 1207.9 TFLOPS（1.28×）**，ncu tensor → 67.9%、`barrier` → 0.18。④**融进上游量化器**（写回前多乘一次 sa，零额外访存）：端到端（量化+GEMM）**0.2617→0.2194 ms（1.19×）**、M=16384 **1.188×**；独立 `prescale_a` pass 只 1.11×。⑤正确性：折后与 fold 相对误差 **0.15%**（数值等价），vs 原始 fp32 参考 ~1.6%（量化误差）。⑥边界：只对 2 的幂 scale 精确、`A'` 与 scale 绑定。代码 `37-fp8-pb-prescale/`（`fp8_gemm_prescale.cu` 主实验、`fp8_gemm_pb2.cu` scale_d 清零、`fp8_gemm_ws.cu` setmaxnreg 负结果；`prescale_M{4096,16384}.out.txt`、`overlap_negative.out.txt`、`scale_d_zeroing.out.txt`、`ncu_fold/out.txt`、`ncu_pt.out.txt`）。**注意：下一篇候选是文章 38（主题 25 Paged KV-cache / flash-decoding）。**

- 2026-09-22：完成并发布 **文章 38（主题 25）Paged KV-cache / flash-decoding 推理注意力**：真实 shape 取自 `/ssd/models/qwen3-8B/config.json`（GQA `Hq=40, Hkv=8, G=5, D=128`；KV 布局 `[num_blocks, P=16, Hkv, D]`），B=64、变长 32k 上下文（KV 读取 8.22 GB）。①**v0 naive**（1 warp/(token,q_head)，KV 直读 G 遍）756 GB/s（22.6%，自读 5× 流量）；②**v1 shared**（1 CTA/(token,kv_head)，协作 smem + 16B 向量化，KV 读 1 遍）1130 GB/s（33.7%）；③**v2 scalar + flash-decoding split** 5.46ms/1506 GB/s（**44.9% 到顶**，ncu `Compute(SM)` **85%**、IPC **3.44**、DRAM 仅 44.7% → **指令发射受限**，`sm__inst_executed` 4.9e9 而有用 FMA 仅 ~6.7e8）；④**v3 tensor-core**：把 G 个 GQA head 打包进 `mma.m16n8k16` 的 M 维（M=16 补齐）、K/V 都存 smem `[p][d]` 行主序（QK 非转置 / PV 转置 `ldmatrix`）、`S` 累加器=PV 的 A 片段（`pack2` 零 shuffle，复用 16 篇）、warp-per-page（CTA 内零 `__syncthreads`）→ **3110.8 GB/s / 2.642ms（92.8% HBM，ncu `DRAM` 93.35%、L2 90.2%、Compute 15%）**，相对 v2 **2.07×**；⑤**扫参**：NW=4 打赢 NW=8/16（smem 34.8KB→3 CTA/SM），split=2×NW4 最佳；B=128/L=8192 达 89%，L=4096 87.3%；⑥**分页开销**：物理页全局打乱只 **−1.3%**（2.771 vs 2.735ms）；⑦**对标 SOTA**（`paged_ref.py`，UNIFORM 同口径）：torch SDPA（稠密 KV）2.687ms/3197 GB/s → 我们 **98.2%**，flash_attn decode 3.278ms/2620 → 我们 **1.20×**，纯 KV 读 2.772ms/3099 → 仅慢 1.4%。⑧正确性：内置独立「gather+两趟 softmax」参考，max_abs_err ~3–6e-4（bf16）。代码 `38-paged-kv/`（`paged_decode.cu`、`paged_ref.py`；`sweep_B64_L32768.out.txt`、`sweep_B64_L4096.out.txt`、`batch_scan_L8192.out.txt`、`smallbatch_scan.out.txt`、`uniform_random.out.txt`、`paged_ref_B64_L32768.out.txt`、`ncu_v3_2x4.out.txt`、`ncu_scalar_v2.out.txt`）。**注意：下一篇候选是文章 39（主题 26 W4A16 dequant-GEMM）或 文章 40（主题 25b MLA decode / FP8 KV-cache）。**

- 2026-09-22：完成并发布 **文章 39（主题 25b）MLA 吸收式 decode + FP8 KV-cache**：真实 shape 取自 `/ssd/models/DeepSeek-V4-Pro/config.json`（`num_attention_heads=128, head_dim=512, qk_rope_head_dim=64, num_key_value_heads=1`；吸收后 `H=128, DC=512, DR=64, DV=512, DK=576`，K/V 同一张 `c_kv`）。`mla_decode_kernel<ROWG,DVGRP,KT,FP8,PIPE,PAGED>`：一 CTA = (batch, head block)，QK 非转置 / PV 转置 `ldmatrix`、`S`=PV 的 A 片段零 shuffle、跨 warp online softmax（`pmax/psum`）、`cp.async` 双缓冲。①**roofline**：`AI=2H(DK+DV)/DK≈242`，贴 H100 bf16 ridge（295）1.2× → **不是纯访存**；内存 roofline 0.72ms、算力 0.59ms。②**最佳 `ROWG=4,DVGRP=2,KT=32` pipe：3.06ms / 779 GB/s / 191.3 TFLOPS（19.3%）** @B=64,L=32768（KV bf16 2.42GB）；ncu：**254 regs、occ 12.5%、DRAM 仅 22.7%、L1/TEX 70.2%、tensor 29.2%、stall `short_scoreboard` 2.52 主导** → 共享内存/发射延迟受限。③**根因**：`O[H][DV]=256KB` fp32 累加器 → `regs=65536/T`，最佳几何 254 regs 锁 1 CTA/SM；`DVGRP=4`（O=64）能 16 warp 但 `KT≥64` 双缓冲 smem 超 227KB 只能退回无 pipe（169 TFLOPS）；且 Q 片段每 tile 重读 36 次（常驻需 144 regs，与 O 撞车）。④**FP8 KV-cache 负结果**：e4m3 per-channel + scale 折进 Q/输出（反量化只剩转换），同配置 bf16 3.10ms → FP8 3.87ms（**慢 25%**）；给出判据——FP8 要赢需算力效率 ≥33%（653 TFLOPS），本 kernel 只有 8%（166 TFLOPS）→ **单换存储不换算力是白费**；对比 38 篇 AI=5 的纯访存 decode 换 FP8 会 ≈2×。⑤分页 FP8 4.26ms（**+10%**，高于 38 篇 GQA 的 1.3%，因 KV 行只有 576B 破坏行内连续）。⑥坑：`KT/DVGRP % 16 != 0` 让 QK 内层 `for(nb<KH/16)` 展开 0 次 → 只算 PV、虚高到 308 TFLOPS（已加 `static_assert`）。⑦正确性：内置两趟 softmax 参考（抽样 3 行），bf16 err 8.2e-3（ref 6.72）、FP8 2.9e-6；per-channel e4m3 量化 `c_kv` 最大相对误差 5.85%。B 扫（L=32k）：B16 3.09ms/47、B64 3.06/188、B128 6.11/191；L 扫（B=64）：4k 0.42/175、32k 3.06/188、128k 12.18/192。代码 `39-mla-decode/`（`mla_decode.cu`；`mla_B64_L32768.out.txt`、`scan_B64_L32768.out.txt`、`mla_B{16,128}_L32768.out.txt`、`mla_B64_L{4096,131072}.out.txt`、`ncu_bf16_best.out.txt`、`ncu_fp8_best.out.txt`）。**注意：下一篇候选是文章 40（主题 25c MLA decode 的 wgmma + FP8 张量核）或 文章 41（主题 26 W4A16）。**

- 2026-09-22：完成并发布 **文章 40（主题 26）W4A16 dequant-GEMM（AWQ/GPTQ 风格）**：真实 shape 取自 `/ssd/models/qwen3-8B/config.json`（`hidden=5120, intermediate=17408`），测 `M=64/128/256, N=17408, K=5120`，`group_size=128`、对称 int4（`q∈[-8,7]` 存 `q+8`，`[N,K/2]` u8 + `[N,K/128]` fp32 scale）。①**v0 朴素两步**（向量化 dequant 物化 bf16 W + cuBLAS bf16）@M=64：dequant 0.0866（223MB 流量 @2575 GB/s）+ GEMM 0.0670（2661 GB/s）= **0.1536 ms**；②**v1 融合** `w4a16_kernel<BM=64,BN,STAGES,KSPLIT>`：`cp.async` 流水 + 主循环内 int4→bf16（**SW128 最小粒度：读一个 `uint32` 写一个 `uint4`**）+ `wgmma.m64n128k16` SS + split-K（`atomicAdd`）→ **0.1114 ms（s3k8，1.38×；有效 int4 权重带宽 400 GB/s）**；③**ncu 判决不是带宽受限**：DRAM **11%**、Compute 26%、L1 22%、occ **6.5%**（86KB smem 锁 2 CTA/SM、156 regs）、主导 stall **fixed-latency execution dependency 37.7%**；④**反量化税 46%**：把 dequant 算术短路（`DQ=false`，结果无意义）得 **0.0613 ms**，差 0.0506 ms；即便去掉 dequant 骨架也才 728 GB/s（22% HBM）；⑤**M 墙（本文最重要结论）**：`Work_dequant ∝ (M/BM)·N·K`，融合只在单 m-tile（`M≤64`）划算——M=128 掉到 0.93×、M=256 0.55×（cuBLAS bf16 @M=256 已 580 TFLOPS/58.7%）；⑥**对标 Marlin 类 SOTA**：有效权重带宽 400 GB/s vs cuBLAS 读 bf16 的 2661 GB/s ≈ **6.7× 差距**（vLLM marlin 无法在本机 build，给上界口径）；⑦split-K 收益：k1 0.136 → k4 0.112（+21%），k8 不再涨。代码 `code/kernel-opt/40-w4a16-gemm/`（`w4a16_gemm.cu`、`w4a16_M{64,128,256}.out.txt`、`ncu_w4a16_fused.out.txt`）。**注意：下一篇候选是文章 41（主题 25c MLA decode wgmma）或文章 42（主题 26b W4A16 warp-specialized dequant）。**

- 2026-09-22：完成并发布 **文章 41（主题 26b）W4A16 的 warp specialization + 波次对齐**：shape 同 40（`/ssd/models/qwen3-8B/config.json`：hidden=5120, intermediate=17408, group=128, 对称 int4）。把 40 的融合骨架拆成 **1 producer WG（`cp.async`+反量化）+ 1 consumer WG（只发 `wgmma`）**，`mbarrier` full/empty 握手：occupancy **6.5%→21%**、regs 156→90、Issue 25.9%→41.5%，但**只快 1.07×**——40 篇的 `fixed-latency 37.7%` 是症状不是根因。①**真正的墙是 wave quantization**：`grid=136×4=544 / 264 槽 = 2.06 → 3 波`，第 3 波仅 16 CTA；同 kernel 换 `N=16896`（grid 528=264×2）**0.1048→0.0842（−20%）**；②按 `T≈ceil(grid/2SM)×(K/BK/KSPLIT)` 选 split-K，k5 预测 48 < k4 的 60，实测 **0.0957ms / 465.6 GB/s（1.17×）**（归因：WS 1.07×、波次 1.08×，可叠加）；③`M=1` 达 **0.0823ms/541.8 GB/s**（vs 40 篇 M1 最佳 0.0897）；④反量化税仍 ~34%（`DQ=false` 地板 0.0696ms/639.8GB/s）；⑤**踩坑**：`empty` mbarrier 超发（每个 consumer warp 的 lane0 arrive 而 count=1）→ 相位错乱、永久自旋、**不报 CUDA error 且 `timeout` 杀不掉**，抽 `mbar_test.cu` 最小复现定位，修法 = count=所有 consumer 线程数 + 全线程 arrive（复现 23 篇结论）；⑥负结果：persistent+`atomicAdd` 派活（0.104≈非 persistent，每项重启流水开销吃掉尾部收益）、PF=2（0.1124）、多 producer WG（0.121）、BN=256（0.1404）、行优先 dequant（8-way bank conflict）。代码 `41-w4a16-ws/`（`w4a16_ws.cu`、`mbar_test.cu`、`ws_M64.out.txt`、`ws_M1.out.txt`、`probe_nodq.out.txt`、`ncu_ws_k5.out.txt`）。**注意：下一篇候选是文章 42（主题 25c MLA decode wgmma + FP8）。**

- 2026-09-22：完成并发布 **文章 50（主题 26i）W4A8 IMMA 的收尾：K-blocked 权重布局 / 持久化负结果**：shape 同 40–48（`/ssd/models/qwen3-8B/config.json`：hidden=5120, intermediate=17408, group=128，权重 int4、激活 int8 per-128）；`N=17408, K=5120, M=1..16`。接 48 的「墙从 ALU 挪到 `long_scoreboard`」。①**先证伪 DRAM 局部性**：用与 GEMM 完全相同的地址流（`64B/2560B` stride）写只读诊断 kernel（`wread_strided_kernel`）→ **2546 GB/s = 顺序读 roof（2575）的 98.8%**，访问模式无罪；②**主功 K-blocked 布局**：权重 `Wp[N][K/2]` → `Wt[g][N][64B]`，一个 stage 的 BN 行从「跨 2560B 的 64B 碎片」变「连续 8KB」，`cp.async` 合并窗口才吃得下 → `long_scoreboard` **1.71→0.30（5.7×）**、DRAM **49%→60%**、寄存器 101→86、occ 22%→26%；重排是一次性 host `memcpy`，smem 内布局不变、IMMA 取数一字不改；③**转置 scale**（`WsT[g][N]`/`AxsT[g][M]` 连续 512B/64B 用 `cp.async` 进 smem、折算从 smem 读）只值 **+4%**；④**`KSPLIT=8` 甜点**（每 CTA 5 个 group，网格 1088），4/5/7/10/16 都更慢。结果 M=1 **0.0283ms / 1672 GB/s / 49.9% HBM**（48 篇同进程 0.0328），M=16 0.0343（48 篇 0.0386），全 M 段总和 **0.872×（−12.8%）**，达纯读 roof 的 **65%**。⑤**负结果**：单 barrier（`one_s3k7` +1.5% 噪声内——`barrier` 高是低 occupancy 的症状）、`BN=256/512`（0.0366/0.055）、**跨 item 持久化全线慢 9–72%**（`pers_c8_s3_p4sm` M1 0.0334、`CHUNK=40` 0.0596）——IMMA tile 已够细无尾波可消、每 group 本就折算、item 边界反加 atomic。数值精确 rel 1e-7。代码 `50-w4a8-imma-pipe/`（`w4a8_imma_pipe.cu`、`sweep_all.out.txt`、`ncu_base48_M1.out.txt`、`ncu_tile_s2k8_b5_M1.out.txt`、`ncu_pipe_s3k7_M16.out.txt`）。**注意：下一篇候选是文章 51（主题 26j：warp-private pipeline 攻 `barrier`）。**

- 2026-09-22：完成并发布 **文章 51（主题 26j）W4A8 IMMA 的 warp-private pipeline——把 barrier 清零之后，墙去哪了**：shape 同 40–50（`/ssd/models/qwen3-8B/config.json`：hidden=5120, intermediate=17408, group=128，权重 int4、激活 int8 per-128）；`N=17408, K=5120, M=1..16`。接 50 的「`barrier` 是第一 stall」。①**warp-private stage 环**：W 按 `warp_col` 沿 N 分片本就互不相交，给每 warp 私有环 + A 私有拷贝，CTA 级 `__syncthreads` 全换 `__syncwarp` → `barrier` **1.61→0.12~0.21（≈8~13×）**；②**A 必须一次性预载**（`WMODE=1`）：per-stage 每 warp 冗余读 A 让 A 流量 ×warp 数（A 字节 ∝ M），`M=16` 反而 **0.0473（比 50 基线慢 30%）**，预载后拉回 **0.0319**；③**墙换名**：`long_scoreboard` **0.30→1.60**（warp 直接暴露在 `cp.async.wait_group` 的 DEPBAR 上），ncu SM Busy 47.6% / DRAM 64.7% / L2 65.4% / occ 29.4% 无一到顶（latency-bound）；④**五个负结果**：紧凑 A（`wC_s2k8` −16%，因 occupancy 被 regs 与 smem 同时限制在 6，缩 smem 无用）、强制 occupancy（`__launch_bounds__(128,10)` → 48 regs + 96B spill，−11%）、`STAGES=6`（smem 122KB→1 CTA/SM，−50%）、`SUB` 打包 stage（打平/更慢，「每字节一次 DEPBAR」不是瓶颈）、`BN=256`/`WN=2`（−5~15%）；⑤结果 M=1 **0.0272ms / 1739 GB/s / 51.9% HBM**（50 篇 0.0283/1672），M=16 **0.0319 / 1486 GB/s / 44.3%**（50 篇 0.0343），同进程全 M 段单配置 **1.097×**；纯读 roof 2590 GB/s（77.3%）的 67%；仍输 45 篇 GEMV（M=1 2408 GB/s/76%）1.35× → **M=1 该用 GEMV、IMMA 价值在 M≥3**。⑥数值：48 点相对误差 1e-7。代码 `51-w4a8-imma-warp/`（`w4a8_imma_warp.cu`、`final_sweep.out.txt`、`sweep_warp4.out.txt`、`ncu_wA.out.txt`、`ncu_wA_s2k8_sec.out.txt`、`ncu_w_s3k7_M1.out.txt`）。**注意：下一篇候选是文章 52（主题 26k：TMA 搬 K-blocked 权重）或文章 49（主题 25d，MLA decode WS，暂缓）。**

- 2026-09-22：完成并发布 **文章 52（主题 26k）W4A8 IMMA 的 TMA warp-private——把 2 KB 权重交给 TMA 引擎（和一个必须自己造的无冲突布局）**：shape 同 40–51（`/ssd/models/qwen3-8B/config.json`：hidden=5120, intermediate=17408, group=128，权重 int4、激活 int8 per-128）；`N=17408, K=5120, M=1..16`。接 51 的「`long_scoreboard` 是第一 stall → 下一步 TMA」。①**每 warp 私有 mbarrier + 1D `cp.async.bulk`**：K-blocked 权重一个 warp 一个 group = 连续 2 KB，一条 bulk 搬完（W 2048B + sa/sc 各 64B + sw 128B，一次 `expect_tx=2304`），warp 自产自消**只需 full barrier、不需 empty barrier**（程序顺序保证）；相位 `(t/STAGES)&1`；②**迎头一棒**：TMA 默认行主序 smem 让 IMMA 读 W 变 8 路 bank conflict（`L1/TEX` 31%→**77%**、shared excess 62%），同进程比 51 慢 5~15%——TMA 不自动等于快；③**自造置换布局** `Wt2[g][warp][c][n][8×u16]`：每 warp 每 group 仍连续 2 KB（TMA 1D bulk 可用），但 `[c][n]` 排列让 8 个 `bgroup` 落 8 个 bank → excess **62%→9%**、`L1/TEX` **35%**、`M=1` 快 1.08~1.10×（干净消融：同 kernel 只切 `PERM`）；④**结果**：逐 M 最优 **0.2198 ms**（51 篇 0.2328，**1.059×**）、单配置 `t_s2k4` 全段 **1.070×**；M=1 `t_s2k10` **0.0257 ms / 1841 GB/s / 54.9% HBM**、M=16 `t_s2k4` 0.0301 / 1572 GB/s / 46.9%；⑤**KSPLIT 随 M 走**（低 M `t_s2k10`、高 M `t_s2k4`，k16/k20 归约税崩），`STAGES=2→3` 低 M 小赚、4/6 反亏，occupancy 被 regs 与 smem 同时锁 6 CTA/SM（强制无收益）；⑥ncu：DRAM **67.2%**/L2 62%/Compute 51%/occ 31.1%/regs 75，**无一级到顶（latency-bound）**；仍输 45 篇 GEMV M=1（2408 GB/s/76.3%）**1.31×** → M=1 用 GEMV、IMMA 价值 M≥3。数值 rel 1e-7。代码 `code/kernel-opt/52-w4a8-imma-tma/`（`w4a8_imma_tma.cu`、`tma2_sweep.out.txt`、`tma3_sweep.out.txt`、`ablation.out.txt`、`final_sweep.out.txt`、`ncu_tma_s2k10_perm.out.txt`、`ncu_tma_s2k10_row.out.txt`、`ncu_tma_s3k8.out.txt`）。**注意：下一篇候选是文章 49（主题 25d，MLA decode WS，暂缓）或主题 26l（把 `[c][n]` 置换布局 + TMA 用到别的低比特算子）。**

- 2026-09-22：完成并发布 **文章 53（主题 18）融合 RoPE —— 交错配对、对半配对，以及一个被访问模式封顶的纯访存算子**：真实语义取自 `/ssd/models/DeepSeek-V4-Pro/inference/model.py:200`（`precompute_freqs_cis` YaRN ramp）与 `:232`（`apply_rotary_emb` 用 `view_as_complex` = **交错配对**），对照 Qwen3/HF 的 `rotate_half`（**对半配对**）。`ds-mla-q`：`T=16384,H=128,HD=512,rd=64`（537 MB）；`qwen3-q`：`T=16384,H=40,HD=128,R=128`（336 MB）。①**现场算 YaRN/`powf`/`sincosf` = 8.3%**（279.7 GB/s），预表后 60%；②**配对方式决定向量宽度**：交错 $(x_{2j},x_{2j+1})$ 是连续 4 B → 一行 rope 当连续 `uint4` 流（一线程 8 元素 = 4 对）+ bf16 表 → **2634.0 GB/s / 78.6%**，等于**同模式 copy roof（79.0%）的 99.5%**；对半 $(x_j,x_{j+d/2})$ 走「半区各一 `uint2`」→ **2750.7 GB/s / 82.1%**（roof 83.8% 的 98.0%）；v2 warp 标量 60.5%/77.4%、elementwise 对半只有 43%（64 位 div/mod）；③**上限是访问模式不是 HBM**：rope 64/512 = 「128 B 块跨 1 KB 步长」，同模式 copy 仅 79%、read 90.7%；把 rope 区**打包连续**同 kernel 立刻 **86.4%**（copy 86.8%）→ **布局差 8 pp**；④**负结果**：表常驻寄存器（表流量 ÷128 head）反而 **62.5%**（< v2 60.5% < v4 78.6%）——表流量从来不是墙（2 MB、L2 命中 53%）；bf16 表 ≈ fp32 表；`uint4`×2/×4 掉到 73%/64%（并行度）；⑤ncu：DRAM **78.45%/81.59%**、L2 70/73%、Compute 26%、occ 91%、`long_scoreboard` 占 stall 九成、30/28 regs；⑥SOTA：torch eager interleaved 117.6 GB/s（**22.4×**）、`rotate_half` 429.6 GB/s（**6.4×**）、torch clone 2994.8 GB/s（89.3%）；对拍 model.py fp64 err 4.0e-3。代码 `53-fused-rope/`（`rope.cu`、`rope_ref.py`、`rope.out.txt`、`packed.out.txt`、`torch_ref.out.txt`、`ncu_v4_ds_q.out.txt`、`ncu_v4s_qwen_q.out.txt`、`dump_*`）。**注意：下一篇候选是文章 54（主题 26l：把「TMA + host 置换布局」推广到别的低比特算子）或文章 55（主题 28：融合 cross-entropy / logits）。**

## 下一步（明确到可执行）

- [x] **文章 39 — 主题 25b：MLA 吸收式 decode + FP8 KV-cache**：**已完成并发布**（见「当前进度」）。真实 shape 取自 `/ssd/models/DeepSeek-V4-Pro/config.json`（`H=128, DC=512, DR=64, DV=512, Hkv=1`）。`mla_decode_kernel<ROWG,DVGRP,KT,FP8,PIPE,PAGED>` 融合 QK/online softmax/PV；最佳 **3.06ms / 779 GB/s / 191 TFLOPS（19.3%）** @B=64,L=32768。**关键发现**：①AI≈242 贴 ridge，**不是纯访存**，ncu DRAM 仅 22.7%、L1/TEX 70.2%、occ 12.5%、`short_scoreboard` 主导；②`O[H][DV]=256KB` fp32 累加器是核心矛盾（254 regs + Q 片段无法常驻）；③**FP8 KV 负结果**（慢 10~25%），给出「FP8 要赢需算力效率 ≥33%」判据（单换存储不换算力是白费）；④坑 `KT/DVGRP%16≠0` → QK mma 展开 0 次（虚高 308）。代码 `code/kernel-opt/39-mla-decode/`。**注意：下一篇候选是文章 40。**
- [x] **文章 40 — 主题 26：W4A16 dequant-GEMM（AWQ/GPTQ 风格）**：**已完成并发布**（见「当前进度」）。真实 shape 取自 `/ssd/models/qwen3-8B/config.json`（`hidden=5120, intermediate=17408`），`M=64/128/256, N=17408, K=5120`，`group_size=128` 对称 int4。`cp.async` 流水 + 主循环内 int4→bf16（SW128，读 u32/写 u4）+ `wgmma` SS + split-K：`M=64` **0.1114ms vs 朴素「物化+cuBLAS」0.1536ms（1.38×）**。ncu DRAM 仅 11%、occ 6.5%、关键时刻是**反量化税 46%**（诊断版 0.0613ms）与 **`Work_dequant ∝ (M/BM)NK`**（M=128/256 反转 0.93×/0.55×）。代码 `code/kernel-opt/40-w4a16-gemm/`。
- [x] **文章 41 — 主题 26b：W4A16 的 warp specialization + 波次对齐**：**已完成并发布**（见「当前进度」）。结论：WS 把 occupancy 6.5%→21%、regs 156→90，但只 **1.07×**；真正的大头是 **wave quantization**（`T≈ceil(grid/2SM)×(K/BK/KSPLIT)`），split-K=5 对齐波次 → **0.0957ms/465.6 GB/s（1.17×）**，`M=1` 达 541.8 GB/s。负结果：persistent 派活、PF=2、多 producer、BN=256。代码 `code/kernel-opt/41-w4a16-ws/`。**剩余**：跨 item 不重启流水的 load-balanced persistent（才能兑现尾部收益）、`m16` 小 tile 打真正的 `M=1`。
- [x] **文章 42 — 主题 25c：MLA decode 的 wgmma 改造 + V 转置税（已完成并发布）**：见「当前进度」。结论与标题预期相反——**纯 wgmma SS 反而更慢**（181.3 < 39 的 189.9），因为 Hopper `wgmma` 的 B 只认 K-major（J4），PV 必须把 `c_kv` 转置成 `V^T[DV,KT]`（每 tile 4096 次跨 1024B 标量读 + SW128 16B 写）。诊断实验（短路 V 重载）给出天花板 **368.2 TFLOPS**，一步锁定瓶颈。解法是**混合**：QK 用 wgmma，PV 用 `mma.m16n8k16 + ldmatrix.x4.trans` **直接从同一块 SW128 K tile 读 V**（SW128 只是 16B 置换、chunk 完好），再叠 **K 双缓冲**（smem 恰好顶格 232448B）→ **290.7 TFLOPS（29.4%）/ 2.009ms**，相对 39 **1.53×**，`long_scoreboard` 2.20→0.10、tensor 19.1%→36.7%。代码 `code/kernel-opt/42-mla-decode-wgmma/`（`mla_decode_hybrid.cu` 主实验、`mla_decode_wgmma.cu` 纯 wgmma、`pvt_test.cu` PV-from-SW128 最小复现、`probe/` 诊断）。
- [x] **文章 43 — 主题 26c：W4A16 的跨 item 持久化流水**：**已完成并发布**（见「当前进度」）。把 `(item,kb)` 展平成一维 stage 流、producer/consumer 跨 item 不停机；`M=1` wave **2.58→1.00**、**0.0788ms/565.5 GB/s（41 篇的 1.04×）**；控制实验证明赢在持久化（同细 chunk 的 baseline KSPLIT=8/16 反而更慢）。交叉点 `M=16~32`，`M≥32` 归约税反超；3 CTA/SM 被寄存器墙否决（ptxas `C7602`）。代码 `43-w4a16-persist/`。
- [x] **文章 44 — 主题 26d：W4A16 的 M=1 GEMV**：**已完成并发布**（见「当前进度」）。算清 `AI≈3.76M`、张量核要到 `M≈78` 才划算，M=1 改带宽最优 GEMV：**0.0284ms / 1566.6 GB/s / 49.7% HBM**，相对 43 篇 tensor-core M=1 **2.81×**、naive **16.1×**。ncu 判决 **ALU 发射受限**（pipe 64.9%），剩余 1.5× gap 在反量化。负结果：免冲突布局/LUT/位技巧/代数化简全负。代码 `code/kernel-opt/44-w4a16-gemv/`。**剩余**：W4A8+dp4a 砍 ALU；`M∈[2,16]` 的 GEMV↔GEMM 自适应。
- [x] **文章 45 — 主题 26e：W4A8 + `dp4a` 补齐 M=1 GEMV 的 ALU gap**：**已完成并发布**（见「当前进度」）。激活 int8（per-128 动态）+ 权重 int4，内层用 `__dp4a` 整数点积把「反量化+MAC」压成一条指令。v1 用 `__vsubss4` 做逐 byte 符号展开被 ptxas 展开成大量 `LOP3/PRMT`（仅 62.4% HBM）；关键一步是**把 nibble 当正整数直接喂 `dp4a`、解析扣掉 `8·Σq^x`**（`Σq^x` 与行无关）→ 指令数 11.87M→7.17M，**0.0185 ms / 2408 GB/s / 76.3% HBM**，相对 44 篇 **1.53×**，ncu 翻转为 DRAM 66.7%。代码 `45-w4a8-gemv/`。
- [x] **文章 46 — 主题 26f：`M∈[1,64]` 的 GEMV↔张量核运行时自适应**：**已完成并发布**（见「当前进度」）。结论：**证伪 44 篇的 $M^\*\approx78$**（真交叉点 M≈2.5 / M≈17）；新写 `mma.sync.m16n8k16` 小 tile，初版无 K-split 惨败（waves 0.26），加 `KSPLIT=8` 后成为 M∈[3,16] 最优；最优由 `P=\lceil M/d\rceil` 决定（GEMV d=1 / mma.b16 d=16 / wgmma d=64）；分派器 12 点求和 = always-wgmma 的 0.838。代码 `code/kernel-opt/46-w4a16-smallm/`。
- [x] **文章 47 — 主题 26g：给 `mma_b16` 小 tile 叠 warp specialization（负结果）+ KSPLIT 并行度**：**已完成并发布**（见「当前进度」）。结论与预期相反：WS 全线更慢——`mma_b16_k8` 78% 的指令是反量化的 ALU+FMA、tensor 仅 2.3%，把反量化搬进 producer WG 指令一条没少（ALU 15.1M→15.0M）、总指令 +9%，`long_scoreboard` **1.77→10.19（5.8×）**。真正的提升来自 **`KSPLIT=8→16`**（waves 2.06→4.12）在 `M∈[3,16]` 一致 **+5~11%**；`mma_b32_k16` 在 M=24 反超 wgmma。新自适应 0.825ms = always-wgmma **1.23×**。代码 `code/kernel-opt/47-w4a16-mma-ws/`。
- [x] **文章 48 — 主题 26h：W4A8 的整数张量核（IMMA）小 M GEMM**：**已完成并发布**（见「当前进度」）。`mma.sync.m16n8k32.s32.s8.u8.s32` + 激活 int8 + 权重 nibble 铺 int8，片段每 lane 一个 word（免 `ldmatrix`），uint16 直读 + `PRMT` + `u8.s8` 免 `-8`，去掉 smem scale 表。M=16 **0.0376ms / 1290 GB/s / 38.5% HBM**（bf16 1.92×），全 M 段 **1.80×**；指令 −59%、`long_scoreboard` 成新墙。代码 `code/kernel-opt/48-w4a8-imma/`。
- [x] **文章 50 — 主题 26i：W4A8 IMMA 的收尾（K-blocked 权重布局 / 持久化负结果）**：**已完成并发布**（见「当前进度」）。三步：①**转置 scale**（`[g][N]` 连续 512B `cp.async`）只 +4%；②**K-blocked 权重布局**（`Wp[N][K/2]`→`Wt[g][N][64B]`，一个 stage 连续 8KB）是主功：`long_scoreboard` **1.71→0.30**、DRAM **49%→60%**、寄存器 101→86；③`KSPLIT=8` 甜点。M=1 **0.0283ms/1672 GB/s/49.9% HBM**（48 篇 0.0328），全 M 段 **0.872×（−12.8%）**，达纯读 roof（2575）的 **65%**。**负结果**：单 barrier 中性、`BN=256/512` 更慢、**跨 item 持久化全线慢 9–72%**（IMMA 无尾波可消、每 group 本就折算）。代码 `code/kernel-opt/50-w4a8-imma-pipe/`。
- [x] **文章 51 — 主题 26j：IMMA 的 `barrier` 墙——warp-private pipeline（每 warp 独立 stage、零 `__syncthreads`）**：**已完成并发布**（见「当前进度」）。`barrier` **1.61→0.12~0.21**（8~13×），但墙换成 `long_scoreboard` **0.30→1.60**（warp 直接暴露在 `cp.async.wait_group` 的 DEPBAR 上）；同进程全 M 段单配置 **1.097×**（M=1 1.07×、M=16 1.11×），M=1 **0.0272ms / 1739 GB/s / 51.9% HBM**、M=16 0.0319 / 44.3%。五个负结果（紧凑 A/强制 occupancy/深 STAGES/SUB 打包/BN=256）逐条实测。代码 `code/kernel-opt/51-w4a8-imma-warp/`。**下一步候选：文章 52（主题 26k）TMA 版 warp-private——用 `cp.async.bulk` + mbarrier 搬 K-blocked 权重（已连续 2KB），把 DEPBAR 等待交给 TMA 引擎。**
- [x] **文章 52 — 主题 26k：warp-private + TMA（`cp.async.bulk`）搬 K-blocked 权重**：**已完成并发布**（见「当前进度」）。每 warp 私有 mbarrier + 1D bulk（连续 2 KB/group），warp 自产自消只需 full barrier；**TMA 默认行主序 smem 撞 8 路 bank conflict**（L1 77%）→ 自造 `Wt2[g][warp][c][n][8×u16]` 置换布局归零（L1 35%）；逐 M 最优 0.2198ms（51 篇 0.2328，**1.059×**）、单配置 1.070×；M=1 0.0257ms/1841 GB/s/54.9%；ncu DRAM 67.2%、无一级到顶。代码 `code/kernel-opt/52-w4a8-imma-tma/`。
- [x] **文章 53 — 主题 18：融合 RoPE（交错/对半配对 + YaRN）**：**已完成并发布**（见「当前进度」）。纯访存算子，v0 现场算 YaRN 仅 **8.3%** → 预表；交错配对当 `uint4` 流 → **78.6%**（= 同模式 copy roof 的 99.5%）、对半配对用「半区各一 `uint2`」→ **82.1%**；**上限是访问模式**（rope 64/512 的 1 KB 步长把 copy roof 压到 79%，打包连续后 86.4%）；负结果：表常驻寄存器更慢（表不是墙）；torch eager 22.4×/6.4×。代码 `code/kernel-opt/53-fused-rope/`。
- [x] **文章 55（主题 28b）— FP8 融合 cross-entropy / LM head**：**已完成并发布**（见「当前进度」）。把 54 的融合 CE 换成 fp8 `m64n128k32`（BK=128）+ TMA SW128，一个 `bool FP8` 参数化两条路径（SW128 atom 逐字节同构）。`sw 8×16` **12.82 ms / 1184.6 TFLOPS（59.9% FP8 峰值）**，同进程 bf16 融合的 **1.95×**、cuBLAS bf16+CE 的 **1.93×**、cuBLAS FP8+CE 的 **1.08×**；ncu 瓶颈 DRAM 24%→11.5%、L2 79%+tensor 73% 双限；logits rel-RMS 3.7% 但 **CE loss 只差 1e-5**。代码 `code/kernel-opt/55-fp8-fused-ce/`。**下一步候选：①主题 28c：真正的 per-128×128 block 量化器 + 真实 outlier 权重上的精度；②用 cluster multicast / 更大 L2 面板攻 79% 的 L2；③把 FP8 融合头用到 MoE 的 down/proj。**
- [x] **文章 56 — 主题 21g：DeepSeek-V4 的路由专家其实是 FP4**：**已完成并发布**（见「当前进度」）。真实 checkpoint 实测 routed experts 是 **FP4 e2m1 + E8M0 block-32**（`expert_dtype: fp4` 覆盖了 `config.json` 的默认 fp8 block-128 描述）；FP4→e4m3 折叠 **逐位无损**（relRMS=0），专家权重 3095GB→1547GB→**773.7GB**；decode GEMV 从算术解码（compute-bound 65.5%，17.5%）经 smem LUT **1.99× 到 0.0100ms/34.8%**，比 fp8 权重路径 **2.16×**。代码 `code/kernel-opt/56-v4-fp4-moe/`。
- [x] **文章 57（优先）— 主题 21h：FP4 无损折叠接回 grouped MoE（prefill）**：**已完成并发布**（见「当前进度」）。结论：现场折与预折叠**逐位相等**，但现场折**慢 1.44~1.51×**（BN=256 11.69 vs 7.76ms）；ncu 两者 Compute 95%/DRAM 47%；**DIAG 地板 FP4 5.25 vs FP8 6.60ms（1.26×）**证明字节红利真实，但变换税 6.4ms > 省下的 2.4ms。**prefill 用 host 预折叠存 FP8，decode 才现场折**。代码 `code/kernel-opt/57-v4-fp4-moe-fold/`。
- [ ] **文章 58（优先）— 主题 21i：host 预排布 FP4 + chunk-local 折叠，兑现 1.26× 地板**：57 证明瓶颈是「手写 SW128 置换 + FP4 折叠」的变换税（6.4ms）。把 SW128 置换搬到 host（一次性重排 packed fp4 成「一个 16B 输入 chunk 恰好对 2 个 SW128 输出 chunk」的布局），kernel 里只剩「读 16B→折→写 32B」的 chunk-local 操作，目标把 $C_\\text{tr}$ 压进 2.4ms、把 FP4 prefill 拉到 ≥1.2×；同时给出 FP4 折叠的位运算版（`(e+6)<<3|m<<2`）替代 32 次/16B 的 LUT 查表。
- [x] **文章 58 — 主题 26m：把 FP4 decode 从 35% 推向 70%+**：**已完成并发布**（见「当前进度」）。结论：**没到 70%，只到 38.7%**——真墙是 `Waves 0.36`/`Issued Ipc 1.08` 的**并行度不足**（`#warp=N/RWW=3072`），不是 bank conflict。唯一有效是**预取深度 1→2**（0.0100→**0.0090 ms/38.7%**）；split-K / PRMT 位运算解码 / 无 smem / 全量预取 / RWW=2 **全负**；LUT↔PRMT 的胜负取决于 M（M=1 LUT、M=4 PRMT）。距 roof 2.2×，判决=形状决定天花板。代码 `code/kernel-opt/58-v4-fp4-decode/`。**剩余**：把 decode 接成 grouped GEMV（多专家合并）改并行粒度。
- [x] **文章 59 — 主题 26n：FP4 decode 的 grouped GEMV**：**已完成并发布**（见「当前进度」）。把一步 decode 的 `B×6` 个 `(token,expert)` 对**按 expert 折叠**进一个 kernel（`grid.y=384`），一个 CTA 吃下某专家全部 m 个 token，权重每专家只读/decode 一次、被 m 个 token 的 `dp4a` 复用；两修复（smem/寄存器随 m 缩放 + 两平面激活布局消 8-way conflict）。**B=256 grouped 2.63ms / 2008 GB/s / 59.9% HBM**（vs batched_m1 3.22×、vs naive 逐对 5.87×）、B=64 达 **82.7%**；ncu 墙从 DRAM 挪到 L1/TEX（60.1% / 83.9%）；PRMT 换 LUT 仍负（慢 33%）。代码 `code/kernel-opt/59-v4-fp4-grouped/`。**下一步候选：文章 60（主题 26o：冲突无关的 LUT / 稀疏路由再压 L1-TEX）、文章 26l（TMA + host 置换布局推广）、文章 49（MLA decode WS，暂缓）。**
- [x] **文章 60 — 主题 26o：FP4 grouped decode 的 LUT 收尾 + 真实稀疏路由**：**已完成并发布**（见「当前进度」）。结论与标题预期相反：`shared_ld` 181.7M / wavefront 475.8M / conflict 244.6M 是真的（LUT 读 = 每 weight byte 一次），但把随机查表换成**结构无冲突的 16 项 nibble 表**（conflict ÷1993、`short_scoreboard` ÷4.7、L1/TEX 83.4→68.8%）后，**墙钟只 +2.9%**——因为墙是 **24% occupancy 带来的 DRAM 延迟**，不是 L1 那级吞吐（DRAM 只 60.4→62.0%）。256 项表的「`uint32`/分半/多副本」全部负结果；`__launch_bounds__(256,3/4)` 溢出（2.95/5.09ms）。**真实稀疏路由**才是模型层杠杆：按活跃专家压缩网格，B=64 幂律（active=168/384、读 44% 权重）**2.086×**、hot-75% **1.24–1.30×**。代码 `code/kernel-opt/60-v4-fp4-grouped2/`。**下一步候选：文章 61（主题 26p）把 grouped GEMV 接进 decode MoE FFN 端到端；或 26q 用 `cp.async` 深流水把权重搬进 smem 解耦 global 延迟。**
- [x] **文章 61 — 主题 26p：decode MoE FFN 端到端（`w1/w3 → SwiGLU → w2`）**：**已完成并发布**（见「当前进度」）。五段流水（quant_x → K1 合并 `[w1;w3]` grouped GEMV → swiglu+per-128 int8 量化 → K2 `w2` grouped GEMV → 加权反置换），B=64 端到端 **5.97 ms / 67.6% 权重 roofline**、对拍 ~1e-7。**关键发现**：①`cp.async` 双缓冲（s2）比寄存器环小赚 3~5%、**DEPTH 3/4 更慢**；②E8M0 uint8 scale（+`__int_as_float(e<<23)`）省 15% 字节只快 5.5%（小 M GEMV 不在纯带宽墙）；③K2 激活 per-pair 不能 gather（`GATHER` 模板），bug 只在 B≥128 暴露。代码 `code/kernel-opt/61-v4-fp4-ffn/`。
- [x] **文章 62（新，优先）— 主题 26q：把 K1 的解码从「每 byte 一次 LUT」换成整 16B chunk 位拼装，并改 K2 的并行粒度**：**已完成并发布**（见「当前进度」）。结论：K1 的墙是 **ALU 发射**（`pipe_alu` 75.9%），把 16 项 nibble 表的 LDS 换成 `prmt` 位拼装（**零 LDS**）正中方向 → **K1 3.641→3.213ms（73.6→83.4% HBM）**、K2 1.941→1.727（69.0→77.6%）、端到端 **5.620→4.968ms（71.8→81.1% 权重 roofline，1.13×）**；LSU 指令 −83%、总指令 −18.5%；ncu 从 ALU 75.9% 变 **DRAM 83.6%/L2 89.6%**。关键：`prmt` selector 只用低 3 位、值 8–15 回绕 → **原始 nibble 可直接当索引**；两条 8 项表（幅值/负值）+ 逐字节符号掩码。K2 并行粒度重扫（RWW/DEPTH/PIPE）全负 → 短 `NITER=3` 的访存延迟是下一堵墙。代码 `code/kernel-opt/62-v4-fp4-ffn2/`。**下一步候选：①K2 用 TMA 搬整 row-tile；②K1 的 L2 89.6%（权重+scale 都过 L2，试 L2 promotion/更大加载粒度/cluster multicast）；③把 PRMT 解码推广回 58/59/60 的单矩阵/grouped GEMV（M≥4 时解码被复用，值得重测）。**
- [x] **文章 63 — 主题 26r：用 TMA 1D bulk 搬整 warp row-tile**：**已完成并发布**（见「当前进度」）。K2 单算子 **1.718→1.506ms（1.14×）**、端到端 **4.979→4.487ms（1.110×）**、roofline **81.0→90.0%**；K1 92.0%/K2 87.9% HBM；ncu K1 DRAM **92.24%**、K2 **90.35%**（occ 24.4% 更低却更快 → TMA 把 latency 从 dependency 拿出）。负结果：`L2::evict_first` 中性、`RWW16/32` 崩、多余 `__syncthreads` 值 3%。代码 `code/kernel-opt/63-v4-fp4-ffn3/`。**下一步候选：文章 64（主题 26s）①把「连续行 → 1D bulk」推广到 prefill grouped GEMM（A tile 连续）；②K1 的 L2 92.4% 墙——cluster multicast / 更大 row-span；③B=256 K1 只有 63.7%（`MTMAX=4` 激活 smem 压 occupancy），试激活分片/流式复用。**
- [ ] **文章 63（新）— 主题 26r：K2 的 TMA row-tile**：K2（`w2 [7168,3072]`）现在是每 lane 8 条 `cp.async` + 短 `NITER=3`，`long_scoreboard` 40.5%、occ 34%。用 `cp.async.bulk.tensor.2d` 一次搬一个 `[BN × WROW]` 的权重 tile（行 stride 1536B，TMA box 内维 512B/128B），producer 用 mbarrier，consumer 只做 `ldsm→prmt 解码→dp4a`；对照 `52` 篇「TMA 默认行主序撞 bank」的教训，GEMV 的读法是 lane 读连续 16B，行主序恰好无冲突。目标 K2 ≥ 85% HBM。

- [ ] **文章 58（候补）— 主题 26l：把「TMA + host 置换布局」推广到别的低比特算子**：26k 的核心可移植结论是「TMA 只决定怎么搬、不决定搬到哪；连续块与无 bank conflict 可同时满足，但布局要自己在 host 排」。可把它用到 48 的 IMMA `BN=256` 或 34/31 的 grouped FP8（那里 W 也是按 expert 连续）。目标：在更大的 `M`/`N` 上验证置换布局是否仍免费。
- [x] **文章 54 — 主题 28：融合 cross-entropy / logits（vocab=129280，DeepSeek-V4）**：**已完成并发布**（见「当前进度」）。split-N TMA+wgmma GEMM + tile 级 online logsumexp + **superblock L2 swizzle**（DRAM 60.21→17.72 GB）；`sw 4×8` **25.07 ms / 605.7 TFLOPS（61.2% 峰值）**，vs cuBLAS+CE 0.98×、eager 0.92×、fp32-logits 1.16×；logits 显存 2.12/4.24 GB → **66 MB**。判决=**显存优化不是吞吐优化**。代码 `code/kernel-opt/54-fused-ce/`。
- [ ] **文章 49（候补）— 主题 25d：MLA decode 的 producer/consumer warp specialization**：42 篇把墙变成「`wait`（等 `ldmatrix`→`mma` 依赖）+ 单 CTA/SM 低 occupancy」。需先有 smem/寄存器几何突破口（231KB smem + 254 regs 锁死 1 CTA/SM），或 Blackwell `tcgen05`。**暂缓**。
- [ ] **文章 49（候补）— 主题 25d：MLA decode 的 producer/consumer warp specialization / MN-major 描述符**：42 篇把墙变成「`wait`（等 `ldmatrix`→`mma` 依赖）+ 单 CTA/SM 低 occupancy」。①生产者 WG 专职 K/V 装载、消费者只做 math；②用 CuTe 生成 MN-major SW128 描述符让 PV 回到 wgmma（J4 认为硬件不支持，需再证）。目标 ≥ 400 TFLOPS。**本轮评估后暂缓**：231KB smem + 254 regs 锁死 1 CTA/SM，V^T 缓冲（65.5KB）塞不进双缓冲 K（295KB），且 47 篇再次证明「把要等结果的访存密集活搬进 producer」会让 `long_scoreboard` 恶化；需先有 smem/寄存器几何的突破口（或 Blackwell）。


- [x] **文章 38 — 主题 25：Paged KV-cache / flash-decoding 推理注意力**：**已完成并发布**（见「当前进度」）。GQA（Qwen3-8B `Hq=40/Hkv=8/D=128`）+ 变长 + paged block table；标量版 44.9% HBM 卡发射口（ncu Compute 85%/IPC 3.44），换 `mma` 把 G 个 head 打包进 M 维 → **92.8% HBM（2.07×）**；对标 torch SDPA **98.2%**、flash_attn decode **1.20×**；分页开销仅 −1.3%。代码 `code/kernel-opt/38-paged-kv/`。
- [ ] **文章 33 — 主题 39 续：MLA 极限冲刺（三）· producer warp 专门化 V 装载 + 单缓冲 vs（暂缓，见「阻塞」）**：32 篇已证 MN-major 免转置不可行、跨迭代软流水被 ptxas 串行化。改走 **warp specialization**：T=384（消费者 2 warpgroup 只做 QK/softmax/P/PV，生产者 1 warp 用 `barrier` 在「PV 刚读完 vs」到「下轮 PV 前」的长窗口里把 V(i+1) gather 进同一块 vs（**单缓冲即可**，无需双缓冲），用 named barrier/`bar.sync` 做 producer↔consumer 握手；关键是把现在「所有线程同步 V 装载」从关键路径拿掉，让消费者立刻进入 QK(i+1)。注意 KT=64 下 vs 单缓冲 65.5KB、smem 已 216KB，须保持 1 CTA/SM。目标 Sk=8k 从 198.8 推向 230+。代码 `code/kernel-opt/33-mla-v-ws/`。**（本轮优先做了更稳的 MoE+FP8=文章 34；本篇因 MLA 的 smem/寄存器硬阻塞暂缓，见「阻塞」，等 1-warpgroup/248-reg 或 Blackwell 再攻。）**
- [x] **文章 37 — 主题 21e：FP8 per-block 的第三条路——把 ue8m0 的 2 的幂 scale 折进操作数**：**已完成并发布**（见「当前进度」）。走上了 1-warpgroup 的路（`scale_d=0` 清零消 `C7514` + `setmaxnreg`），但 ptxas 报 `C7507` 忽略 `setmaxnreg`、ping-pong 仍 348（基线 940）。改走**折 scale**：`ue8m0` 是 2 的幂 → `A_fp8×sa` 精确 → per-block GEMM 退化成 per-tensor GEMM，**940.6 → 1207.9 TFLOPS（1.28×）**；融进上游量化器后端到端 **1.19×**，数值与 fold 差 0.15%。代码 `code/kernel-opt/37-fp8-pb-prescale/`。**这也回填了 24/31/34/36 的公共瓶颈（折 A/B 后切 per-tensor 路径即可）。**
- [x] **文章 36 — 主题 17b：compressor 投影的 L2 墙 · 三种压 L2 手段全负 + FP8 化**：**已完成并发布**（见「当前进度」）。翻案 35 的 L2 判决（最佳几何是 tensor 76% 受限），multicast/persisting/K-split/fence 全负，FP8 per-block 959/974 TFLOPS（bf16 的 1.39×）。代码 `36-dsa-compressor-mcast/`。
- [x] **主题 17 剩余：DSA compressor（ratio ∈ {4,128,0}）**：**已完成并发布为文章 35**（见「当前进度」）。合并投影 650–655 TFLOPS（65–67%，cuBLAS 的 ~80%）+ 融合池化 83–84% HBM，端到端 2.0–2.4×；投影被 L2 卡住（见「下一步」）。
- [ ] **文章 36（新）— 主题 17b：compressor 投影的 L2 墙 —— B 侧 cluster multicast / 分块 L2 常驻**：已做，**已完成并发布为文章 36**（见「当前进度」）。结论与标题相反：三种压 L2 手段全负，真正提速来自 FP8。
- [x] **文章 34 — 主题 21d：fused MoE + per-block FP8（grouped per-block up/gate + swiglu/quant + down）**：**已完成并发布**（见「当前进度」）。把 31 的 per-block grouped 接进 30 的 MoE FFN 端到端：权重 50.7→**25.4 GB**，端到端相对 bf16 **1.92×（M8k bal）/ 2.21×（M16k）/ 2.06×（M8k rand）**；K2 down **88.1% HBM**。两个负结果：①**unpermute 融合在 FP8 下失效**（K2f 3.83 vs 3.35+0.54，L2 94.4%/DRAM 73.2%）——GEMM 变快后散写不再被藏住；②**per-block + 双累加器 SwiGLU = 256>255 寄存器**，`BN=256` 崩到 21.7 ms。附带 swiglu+动态量化从 39%→**92% HBM**（一行一 block/float4/warp amax）。代码 `34-fused-moe-fp8/`。
- [x] **文章 25 — 主题 21 MoE（二）：grouped GEMM**：已完成并发布（见「当前进度」）。产出：①单 kernel 把 expert 编码进 B 的行坐标 `b_row=group*N+n`，A/B 均 2D TMA，复用 23 的 TMA+mbarrier+warp specialization；②contiguous prefill 相对 per-expert loop **3.59×/2.36×/1.47×**（tokens=8k/16k/32k），最佳 958–1008 TFLOPS（aligned，48–51%），反超 cuBLAS per-expert graph loop **1.17×**；③masked decode **3.02ms / 2.8TB/s（83.6% HBM）**，比 cuBLAS padded batched 快 1.53×；④contiguous 对齐 padding 实测 32.5%/19.8%/10.8%；⑤ncu 定位下一堵墙是 **L2 81.9%**（B 被每 m-tile 重读，放大 ~4.5×）。代码 `25-moe-grouped-gemm/moe_grouped.cu`。
- [x] **文章 26 — 主题 21b MoE（二·续）：TMA cluster multicast 压 L2**：已完成并发布（见「当前进度」）。结论：A 广播协议跑通、L2 读 sector −18.8%，但只在 32768 tokens+128×128 赢 **+4.3%**，小 batch/masked 反而变慢；根因是 `lts__throughput` 是请求/延迟而非字节。**剩余**：B 侧 multicast（沿 M 的 cluster + `is_peer_cta_alive` 动态回退）、以及把 multicast 与 per-block FP8 缩放叠加，见上一条 28。
- [x] **文章 27 — 主题 20 MoE（一）：router + top-k + permutation**：已完成并发布（见「当前进度」）。
- [x] **文章 28 — 主题 20b MoE（一·续）：`wgmma`/TMA 版 gate GEMM**：已完成并发布（见「当前进度」）。结果：`wgmma` SS + SW128 → 337/360 TFLOPS；再叠 TMA + warp specialization → **565.4@16k / 600.3@32k**，达 cuBLAS **77.0%/79.8%**，相对 27 篇 **2.80×/2.29×**。**剩余**：A 被 3 个 n-tile 重读（ncu L2 79.1%）→ 见下一条 cluster multicast。
- [x] **文章 29 — 主题 20b-2：gate GEMM + TMA cluster multicast**：已完成并发布（见「当前进度」）。结果：**A-multicast 证伪了 28 篇「A 是墙」的猜测**（L2 读 sector −26% 但慢 23%，瓶颈是 tensor）；**B-multicast CN=2 小幅赢（+5.6%）但输给直接放大 BM**；wide-N 撞寄存器墙。产出 multicast 判据表。**剩余**：`lts__throughput`/耦合的定量模型、B 侧 CN=2 与 per-block FP8 缩放叠加（见 30/21c）。
- [x] **文章 30 — 主题 20c MoE（三）：fused MoE（gather 版 grouped GEMM + SwiGLU + unpermute）**：已完成并发布（见「当前进度」）。把五段流水融成 3 kernel（K1 gather+SwiGLU、K2 down+unpermute scatter）；端到端 1.02–1.08×、activation 流量 −81%；单 kernel K2 **898 TFLOPS（90.8% 峰值）**。关键发现：prefill FFN 是**权重带宽受限**（50.7 GB 权重 vs 1.8 GB activation），融合收益被 activation 占比锁死。
- [ ] **文章 31b（候补）— 30 篇遗留：K2 的 L2 墙（88%）**：K2 bn256 的 A2 被 `H/256=28` 个 n-tile 重读，ncu L2 88.1%。可叠 TMA cluster multicast 沿 N 广播 A（26/29 的判据：瓶颈在字节、BM 无法再放大时才值得），或试 BM=256 的 down tile 把 A 重读再砍半（注意 24 篇的寄存器账）。
- [ ] **文章 26b（候补）— B 侧 multicast**：沿 M 组 cluster 广播 B，群内需 DeepGEMM 式 `is_peer_cta_alive`（同 expert 才广播），并在 128×256 几何上验证（那里 B 才是大头）；注意 `grid.y % CM != 0` 时（16k/32k 的 `M_total/BM` 为奇数）需先 pad 行。
- [ ] **DeepGEMM 对照**（阻塞项，见「阻塞」）：需要容器补 `elfutils/libdwfl-dev` 才能编译 `~/github/DeepGEMM/_C`，以给出 grouped GEMM 与 SOTA 的精确差距。

- [x] **16 MLA 注意力（二）**：已完成（见「当前进度」）。
- [x] **文章 17 — 主题 24 Muon / MuonClip 的 Newton–Schulz 正交化**：已完成并发布（见「当前进度」）。
- [x] **文章 18 — 主题 17（上半）DSA lightning indexer + exact top-k**：已完成并发布（见「当前进度」）。
- [x] **文章 19 — 主题 17（下半）DSA 稀疏 MLA 消费端**：已完成并发布（见「当前进度」）。剩余 **compressor（ratio 4/128/0）** 未做，列入 backlog。
- [x] **文章 20 — 主题 16b/31（已完成）MLA 极限冲刺（一）：SW128 swizzle + wgmma**：把 `wgmma` 的操作数 smem 布局从无 swizzle 的 K-major `INTERLEAVE` 换成 **K-major SW128（`layout_type=1` + `c'=c^r` 物理布局）**。①用 CUTLASS canonical 布局推导描述符（`LBO=1`、`SBO=(K/64)*1024`、k16 步进 `floor(s/4)*1024+(s%4)*32`）；②小 GEMM 冒烟验证 → **132 TFLOPS @4096³**；③QK576/PV 定点测试分别锁死；④移植回融合 MLA：INTERLEAVE 84.6 → **105.3 / 112.9 / 119.9 TFLOPS（Sk=1k/2k/4k，+24.5%）**；⑤踩坑：nvcc 合并相邻 bf16 标量存储丢高 16 位（3553 字节错位），改 `pack2` u32 修复。ncu：store bank conflict 2.7e8→4.4-way、L1 86%→70.8%，但 221KB smem 锁 1 CTA/SM（occ 12.5%、No Eligible 80.9%、47.8% 停 L1TEX）→ 仍 1.62× 慢于 `f4s`、距 FlashMLA ~640 约 6.1×。代码 `20-mla-wgmma-sw128/`。
- [x] **文章 21 — 主题 32/33/39（上半）MLA 极限冲刺（二）：修 V 转置访存 + `cp.async` 单缓冲预取 K**：已完成并发布（见「当前进度」）。做了两件事，均实测：①V 转置迭代顺序 `dv` 最快 → global 读合并（`l1tex` 73%→46%，105.3→142.3）；②`cp.async` 在 softmax/P/PV 期间预取下一块 K（K 只被 QK 读，`wgmma.wait_group` 后即可覆盖，**单缓冲无需双缓冲**）→ 159.8/193.2/198.8（Sk=1k/4k/8k），Sk≥4096 反超 `f4s`，距 FlashMLA ~3.3×。**TMA/双缓冲/warp specialization 仍未做**（单缓冲约束让 V 的延迟藏不住、L1 46% + barrier 是下一道墙）。
- [x] **文章 22 — 主题 22 FP8 GEMM（一）：e4m3 的 mma.sync 与 wgmma + per-tensor / per-block 缩放**：已完成并发布（见「当前进度」）。
- [x] **文章 23 — 主题 23/32/33 FP8 GEMM（二）：TMA + mbarrier + warp specialization**：已完成并发布（见「当前进度」）。
- [ ] **其他模型场景**：RoPE/RMSNorm/compressor…（见上）。模型类文章优先；20 的 SW128 与主题 31/38/39 共用基础设施，是性能主线。
  - **编号说明**：「系列大纲」里的数字是**主题编号**（稳定 ID），文章 `NN` 是**发布顺序**。主题 24（MuonClip）= 文章 17、主题 17 上半 = 文章 18、下半 = 文章 19、主题 22（FP8 GEMM 一）= 文章 22、主题 23/32/33（FP8 GEMM 二 · TMA）= 文章 23、主题 23b（FP8 GEMM 三 · per-block 寄存器墙）= 文章 24、主题 21（MoE grouped GEMM）= 文章 25、主题 21b（cluster multicast）= 文章 26、主题 20（MoE router）= 文章 27、主题 20b（gate wgmma/TMA）= 文章 28、主题 20b-2（gate cluster multicast）= 文章 29、主题 20c（fused MoE）= 文章 30、主题 21c（MoE grouped + per-block FP8）= 文章 31、主题 19（RMSNorm/QK-Norm）= 文章 32、主题 21d（fused MoE + per-block FP8）= 文章 34、主题 17 剩余（DSA compressor）= 文章 35、主题 17b（compressor 投影压 L2 + FP8）= 文章 36、主题 21e（FP8 per-block 折 scale）= 文章 37、主题 25（Paged KV-cache / flash-decoding decode 注意力）= 文章 38、主题 25b（MLA 吸收式 decode + FP8 KV-cache）= 文章 39、主题 26（W4A16 dequant-GEMM）= 文章 40、主题 26b（W4A16 warp specialization + 波次对齐）= 文章 41、主题 25c（MLA decode 的 wgmma 改造 + V 转置税）= 文章 42、主题 26c（W4A16 跨 item 持久化流水）= 文章 43、主题 26d（W4A16 的 M=1 GEMV）= 文章 44、主题 26e（W4A8+`dp4a`）= 文章 45、主题 26f（小 M 的 GEMV↔张量核分派）= 文章 46、主题 26g（W4A16 小 M 的 WS 负结果 + KSPLIT 并行度）= 文章 47、主题 26h（W4A8 的整数张量核 IMMA 小 M GEMM）= 文章 48、主题 26i（W4A8 IMMA 收尾：K-blocked 布局 / 持久化负结果）= 文章 50、主题 26j（warp-private pipeline）= 文章 51、主题 26k（TMA warp-private + `[c][n]` 置换布局）= 文章 52、主题 18（融合 RoPE + YaRN）= 文章 53。**下一篇候选：文章 54**（主题 26l：把「TMA + host 置换布局」推广到别的低比特算子）**、文章 55**（主题 28：融合 cross-entropy / logits）**或文章 49**（主题 25d：MLA decode 的 producer/consumer WS，需先有 smem/寄存器突破口）。发布时把「文章号 ↔ 主题号」记进「当前进度」。

## 灵感 / backlog（想到就记，别丢）

- **MLA MN-major（免 V 转置）**：21 篇盲猜 GMMA MN-major 描述符失败（cute 的 LBO/SBO 与硬件语义相反、结果错乱）。正确做法是用 CUTLASS CuTe `make_tiled_mma` + `partition`/`copy` 让库生成 smem 布局与描述符，再对照手动版；成功后可消掉 21 篇没藏住的 V 转置/预取开销。
- **MLA V 的 TMA 预取**：单缓冲限制让 V 的 global 延迟藏不住（长上下文 `long_scoreboard` 残留）。要么双缓冲（需先给 Q 腾 smem），要么用 TMA + mbarrier 把 V 也做成 producer/consumer。
- **消 QK dup / P 走 RS**：`wgmma` 版两个 warpgroup 各算一份完整 S（dup 2×）；改 n32 分工或让 P 从寄存器直接喂 PV（RS）可省 smem 往返与一次 `__syncthreads`。
- **FP8 per-block 的 1-warpgroup / 248-register 布局**（24 篇遗留）：DeepGEMM 用 BLOCK_M=128、**1 个 math warpgroup（128 线程）**、`setmaxnreg` 给到 248 寄存器，把 `final_accum(128)+accum(64)=192` 塞下且不 spill。我们当前 2 WG/288 线程只有 227 上限，per-block 必掉 1 CTA。试这条路看能否把 per-block 从 936 推向 1100+；配套 CUTLASS `warpgroup_fence_operand` + `warpgroup_wait<0>` 避开 ptxas 串行化。
- **FP8 per-block · TMA cluster multicast**：同一 A tile 在 N 方向广播（`cp.async.bulk.tensor.2d...multicast::cluster`），砍 L2→SM 重复装载（24 篇 per-block L2 65%）。可先在 per-tensor 上验证收益。
- **Blackwell tcgen05 对照**：tcgen05 累加器放 tensor memory（TMEM），彻底不吃寄存器，per-block 的寄存器墙消失。若有 sm_100 机器，重跑本 shape 对照。
- **DSA compressor**（主题 17 剩余）：ratio ∈ {4,128,0} 的 KV 压缩算子，V4-Pro/Lite 的层间压缩比不同，用来把超长上下文压成更少的 key，再接 indexer/top-k。
- DeepGEMM 的 JIT + contiguous grouped GEMM 与 DeepSeek-V4 的 expert 分布对齐。
- FlashMLA 的 split-KV + `m64n...` 细节，和本文 16 逐段对照。
- ~~MuonClip 的 Newton–Schulz 5 步迭代（zeropower_via_newtonschulz）在 N=4096 上的 roofline：它到底是计算受限还是访存受限？~~ → **已答（文章 17）**：算力受限（DRAM 13%），瓶颈是自研 `mma` GEMM 的 L2 放大 + 寄存器墙；下一步 wgmma/TMA。
- 用 nsys 把「GEMM + 两个 epilogue」与「融合 GEMM」对比端到端。
- 把每篇 kernel 存成 v0/v1/v2 版本序列，做「优化步骤 diff 视图」。
- 给 01 roofline 画 mermaid 图。
- 复现 te-perf 文章里的生产 shape，把算子级结论接回模型级。
